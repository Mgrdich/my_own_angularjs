# Tasks: `$sce` — Strict Contextual Escaping

- **Specification:** `context/spec/012-strict-contextual-escaping/`
- **Status:** Draft

---

- [ ] **Slice 1: Trusted-Value Class Hierarchy + `SCE_CONTEXTS` Constant**
  - [ ] Add path alias `@sce/*` → `src/sce/*` in `tsconfig.json`, `vitest.config.ts`, and `rollup.config.*`. Add `@sce` to the `no-restricted-imports` list in `eslint.config.*` (mirroring `@core`, `@parser`, `@di`, `@interpolate`). **[Agent: rollup-build]**
  - [ ] Create `src/sce/sce-contexts.ts` exporting `SCE_CONTEXTS = { HTML: 'html', URL: 'url', RESOURCE_URL: 'resourceUrl', JS: 'js', CSS: 'css' } as const`, the `SceContext` TS union type, and a runtime `isValidSceContext(v: string): v is SceContext` guard. Also define an internal `SCE_CONTEXT_ANY = '$$ANY$$' as const` (module-private — do NOT re-export from `index.ts`). **[Agent: typescript-framework]**
  - [ ] Create `src/sce/trusted-values.ts` with: abstract `TrustedValue` class (`readonly $$unwrapTrustedValue: string`, `toString()` returns the raw string), concrete subclasses `TrustedHtml`, `TrustedUrl`, `TrustedJs`, `TrustedCss`, `TrustedResourceUrl` (`extends TrustedUrl` — AngularJS parity so a trusted resource URL is accepted where a trusted URL is expected but not vice-versa), internal `TrustedValueAny` (not exported), and guards `isTrustedValue(v)` and `isTrustedFor(ctx, v)`. `isTrustedFor` must honor the `TrustedResourceUrl extends TrustedUrl` subtype rule and also accept `TrustedValueAny` for every context. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/sce-types.ts` with scaffolding types: `SceContext` (re-exported from `sce-contexts`), `ResourceUrlListEntry = 'self' | string | RegExp`, placeholders `SceDelegateOptions`, `SceOptions`, `SceDelegateService`, `SceService` (concrete shapes filled in in later slices). **[Agent: typescript-framework]**
  - [ ] Create `src/sce/index.ts` re-exporting `SCE_CONTEXTS`, the `Trusted*` classes, `TrustedValue`, `isTrustedValue`, `isTrustedFor`, and the public types. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/__tests__/trusted-values.test.ts` covering: `toString()` / `String(wrapper)` returns the raw string; `instanceof` chain (`TrustedHtml instanceof TrustedValue`); `TrustedResourceUrl` is also `instanceof TrustedUrl` (subtype); `TrustedUrl` is NOT `instanceof TrustedResourceUrl`; `isTrustedFor('html', new TrustedHtml('x')) === true`; `isTrustedFor('url', new TrustedResourceUrl('x')) === true`; `isTrustedFor('resourceUrl', new TrustedUrl('x')) === false`; `isTrustedFor('html', new TrustedUrl('x')) === false`. **[Agent: vitest-testing]**
  - [ ] Create `src/sce/__tests__/sce-contexts.test.ts` covering: `SCE_CONTEXTS` keys and values; `isValidSceContext` accepts all five strings and rejects `'$$ANY$$'`, `'bogus'`, `''`, and non-string inputs. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 2: Resource-URL Matcher (Pure Helper)**
  - [ ] Create `src/sce/resource-url-matcher.ts` with: `compileMatchers(list: ResourceUrlListEntry[]): CompiledMatcher[]` (each matcher is either `{ kind: 'self' }`, `{ kind: 'regex', pattern: RegExp }` from a user-supplied `RegExp`, or `{ kind: 'regex', pattern: RegExp }` compiled from a string pattern); the string-pattern compiler anchors with `^…$`, escapes regex metacharacters except `*`, translates `**` → `.*`, and `*` → `[^:/?#]*`; throws on invalid entry types. Export `matches(url: string, matchers: CompiledMatcher[], baseUrl?: string): boolean` and `isSameOrigin(url: string, baseUrl: string): boolean`. `'self'` matching resolves the URL via `new URL(url, document.baseURI)` and compares `{protocol, host, port}` against `document.location`. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/__tests__/resource-url-matcher.test.ts` (uses the Vitest jsdom env) covering: string pattern `'https://api.example.com/**'` matches `'https://api.example.com/v1/users'`; `*` does NOT cross `/` (e.g. `'https://api.*.com/x'` matches `'https://api.my.com/x'` but not `'https://api.my.corp.com/x'`); `**` DOES cross `/`; `'self'` matches relative and same-origin absolute URLs; `'self'` rejects cross-origin URLs; protocol-relative URL (`//other.com/x`) resolves against `document.baseURI`; user-supplied `RegExp` is matched against the full URL string; empty list → nothing matches; invalid entry types (number, plain object, boolean) throw at `compileMatchers` call time. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 3: `createSceDelegate` ES Factory (Always Strict)**
  - [ ] Flesh out `SceDelegateOptions` and `SceDelegateService` in `src/sce/sce-types.ts`: `SceDelegateOptions = { trustedResourceUrlList?: ResourceUrlListEntry[]; bannedResourceUrlList?: ResourceUrlListEntry[] }`; `SceDelegateService = { trustAs(ctx, value): unknown; getTrusted(ctx, value): unknown; valueOf(value): unknown }`. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/sce-delegate.ts` exporting `createSceDelegate(options?: SceDelegateOptions): SceDelegateService`. Defaults: `trustedResourceUrlList = ['self']`, `bannedResourceUrlList = []`. Compile both lists via `compileMatchers(...)` at factory-call time. Return a plain object with `trustAs`, `getTrusted`, `valueOf` methods closed over the compiled matchers. Rules: `null`/`undefined` pass through for both `trustAs` and `getTrusted`; non-string input to `trustAs` throws with `typeof` in the message; unknown `ctx` throws; `trustAs` on already-trusted value re-wraps for the requested context; `getTrusted` on `url` context accepts plain strings (AngularJS parity); `getTrusted` on `resourceUrl` consults lists (block-first, then allow) before falling through to the wrapper check; every other context requires `isTrustedFor(ctx, value)`; errors are `Error` instances with descriptive messages naming the context and (for `resourceUrl`) which check failed. **[Agent: typescript-framework]**
  - [ ] Add `createSceDelegate` and `sceDelegate = createSceDelegate()` (pre-configured default export) to `src/sce/index.ts`. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/__tests__/sce-delegate-esm.test.ts` covering: `createSceDelegate()` defaults (allow=`['self']`, block=`[]`); `trustAs('html', 'x')` returns `TrustedHtml`; nullish pass-through; non-string throws naming `typeof`; unknown context throws; `trustAs('url', trustAs('html', 'x'))` returns `TrustedUrl` (re-wrap); `getTrusted('html', trustedHtml)` returns the string; `getTrusted('url', trustedHtml)` throws (context mismatch); `getTrusted('url', 'http://example.com')` returns the string unchanged (URL pass-through); `getTrusted('resourceUrl', ...)` with allow-matching URL → returns string; with non-matching URL → throws "did not match"; with both-matching → throws "matched banned" (block precedence); `valueOf(trustedHtml)` returns the string; `valueOf('plain')` returns `'plain'`. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 4: `createSce` ES Factory + `sce` Default Export**
  - [ ] Flesh out `SceOptions` and `SceService` in `src/sce/sce-types.ts`: `SceOptions = { delegate?: SceDelegateService; enabled?: boolean }`; `SceService` exposes `isEnabled()`, generic `trustAs`/`getTrusted`/`parseAs`, and the 15 per-context shortcut methods (`trustAsHtml` / `…Url` / `…ResourceUrl` / `…Js` / `…Css`, same for `getTrusted*` and `parseAs*`). All methods bound at construction so the service is safely destructurable. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/sce.ts` exporting `createSce(options?: SceOptions): SceService`. Defaults: `delegate = createSceDelegate()`, `enabled = true`. When `enabled === false`: `trustAs` returns the value unchanged (no wrapper created), `getTrusted` returns `delegate.valueOf(value)` (strips wrapper if present, otherwise pass-through), `valueOf` still delegates normally. `parseAs(ctx, expr)` imports `parse` from `@parser/index`, calls it once, and returns a fn `(scope, locals?) => this.getTrusted(ctx, parsed(scope, locals))` with `.literal`/`.constant`/`.oneTime` copied from the inner `ExpressionFn` via `Object.defineProperties`. Validate context for `trustAs`/`getTrusted`/`parseAs` via `isValidSceContext`. **[Agent: typescript-framework]**
  - [ ] Add `createSce` and `sce = createSce()` to `src/sce/index.ts`. Extend `src/index.ts` to re-export `createSce`, `sce`, `createSceDelegate`, `sceDelegate`, `SCE_CONTEXTS`, all `Trusted*` classes, `isTrustedValue`, `isTrustedFor`, and the public types (`SceContext`, `SceService`, `SceDelegateService`, `SceOptions`, `SceDelegateOptions`, `ResourceUrlListEntry`). **[Agent: typescript-framework]**
  - [ ] Create `src/sce/__tests__/sce-esm.test.ts` covering: `createSce()` defaults — `isEnabled() === true`; each of the 15 shortcuts delegates correctly (shortcut output === generic output); strict OFF (`createSce({ enabled: false })`) — `trustAs('html', 'x') === 'x'` (no wrapper), `getTrusted('html', 'x') === 'x'`, no errors; strict ON — `getTrusted('html', 'plain')` throws, `getTrusted('html', trustAsHtml('x')) === 'x'`; `parseAs('html', 'user.bio')(scope)` unwraps `TrustedHtml` at `scope.user.bio`; `parseAs` preserves `.literal`, `.constant`, `.oneTime` from `parse()`; destructurable: `const { trustAsHtml } = sce; trustAsHtml('x')` works; explicit delegate injection — `createSce({ delegate: customDelegate })` routes through it. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 5: Provider Shims + `ng` Module Registration + DI Integration**
  - [ ] Create `src/sce/sce-delegate-provider.ts` exporting the `$SceDelegateProvider` class. Instance state: `$$allowList: ResourceUrlListEntry[] = ['self']`, `$$blockList: ResourceUrlListEntry[] = []`. Overloaded getter/setter methods `trustedResourceUrlList()` / `trustedResourceUrlList(list)` and `bannedResourceUrlList()` / `bannedResourceUrlList(list)` — getters return a shallow copy, setters validate entries, store a defensive copy, return `this`. `$get = [(): SceDelegateService => createSceDelegate({ trustedResourceUrlList: this.$$allowList, bannedResourceUrlList: this.$$blockList })] as const`. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/sce-provider.ts` exporting the `$SceProvider` class. Instance state: `$$enabled: boolean = true`. Overloaded `enabled()` / `enabled(value)` — getter returns current flag, setter validates boolean, stores, returns `this`. `$get = ['$sceDelegate', (delegate: SceDelegateService): SceService => createSce({ delegate, enabled: this.$$enabled })] as const`. **[Agent: typescript-framework]**
  - [ ] Extend `src/core/ng-module.ts`: add `.provider('$sceDelegate', $SceDelegateProvider)` and `.provider('$sce', $SceProvider)` to the chain. Extend the `declare module '@di/di-types'` augmentation to include `$sceDelegate`/`$sce` in `registry` and `$sceDelegateProvider`/`$sceProvider` in `config`. Re-export `$SceProvider` and `$SceDelegateProvider` from `src/sce/index.ts`. **[Agent: typescript-framework]**
  - [ ] Create `src/sce/__tests__/sce-delegate-provider.test.ts` covering: default lists; fluent chaining `provider.trustedResourceUrlList([...]).bannedResourceUrlList([...])`; getter returns a copy (mutating it doesn't affect the provider); setter validates entries at call time (invalid entry throws synchronously); `$get` invocation produces a working `SceDelegateService`. **[Agent: vitest-testing]**
  - [ ] Create `src/sce/__tests__/sce-provider.test.ts` covering: `enabled() === true` by default; `enabled(false)` returns `this` (fluent); `enabled()` now returns `false`; `enabled(true)` restores; `$get` invocation with a mock `$sceDelegate` produces a `SceService` with `isEnabled()` reflecting the captured flag. **[Agent: vitest-testing]**
  - [ ] Create `src/sce/__tests__/sce-di.test.ts` covering: `createInjector([ngModule])` exposes both `$sce` and `$sceDelegate`; `$sceProvider` / `$sceDelegateProvider` accessible only during `config()` phase (accessing them post-config throws, per spec 008); `config(['$sceDelegateProvider', p => p.bannedResourceUrlList(['https://bad.com/**'])])` is observed at runtime by `$sce.getTrustedResourceUrl`; `config(['$sceProvider', p => p.enabled(false)])` produces a pass-through `$sce`; **parity guard:** for the same inputs, DI-resolved `$sce` and the ESM `sce` default export produce identical outputs (sample: `trustAsHtml`, `getTrustedHtml`, `parseAsHtml` round-trip). **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 6: `$interpolate` Integration — Wire `trustedContext` to `$sce`**
  - [ ] Extend `src/interpolate/interpolate-types.ts`: add `sceGetTrusted?: (ctx: SceContext, value: unknown) => unknown` and `sceIsEnabled?: () => boolean` to `InterpolateOptions`; narrow the `trustedContext` parameter on `InterpolateService` call signatures from `string` to `SceContext | undefined`. Type-only import of `SceContext` from `@sce/sce-types`. **[Agent: typescript-framework]**
  - [ ] Modify `src/interpolate/interpolate.ts`: remove the `TODO(spec-$sce)` comment and `void trustedContext;` no-op. When `trustedContext !== undefined`, validate via `isValidSceContext` (throw on unknown). **Compile-time single-binding check:** if `trustedContext !== undefined` AND `sceIsEnabled?.() === true` AND the template has either non-empty literal text segments OR more than one expression, throw a descriptive `"interpolations in trusted contexts must have exactly one {{expression}} and no surrounding text: <text>"` error including the context name. A literal-only template with `trustedContext` is allowed (nothing to sanitize). **Render-time:** when `trustedContext` is set AND the compile-time check passed AND there is exactly one expression, route its evaluated result through `sceGetTrusted!(trustedContext, value)` BEFORE stringification. Interactions with `mustHaveExpression`, `allOrNothing`, `oneTime`, and escape sequences are preserved. **[Agent: typescript-framework]**
  - [ ] Modify `src/interpolate/interpolate-provider.ts`: change `$get` to declare `$sce` as a dep and pass `sceGetTrusted` / `sceIsEnabled` callbacks: `$get = ['$sce', ($sce: SceService): InterpolateService => createInterpolate({ startSymbol: this.$$startSymbol, endSymbol: this.$$endSymbol, sceGetTrusted: (ctx, v) => $sce.getTrusted(ctx, v), sceIsEnabled: () => $sce.isEnabled() })] as const`. **[Agent: typescript-framework]**
  - [ ] Create `src/interpolate/__tests__/interpolate-sce.test.ts` covering: **Strict ON, HTML:** `$interpolate('{{trustedValue}}', false, 'html')(scope)` returns the unwrapped string when `scope.trustedValue` is a `TrustedHtml`; `$interpolate('Hello {{trustedValue}}', false, 'html')` throws at compile time (single-expression violation, literal prefix); `$interpolate('{{a}} {{b}}', false, 'html')` throws at compile time (multiple expressions); `$interpolate('{{trustedValue}} suffix', false, 'html')` throws at compile time (literal suffix); plain-string scope value: `$interpolate('{{x}}', false, 'html')(scope)` throws at render time (trust violation). **Strict OFF:** `$interpolate('Hello {{name}}', false, 'html')(scope)` works like spec-011 (literal text allowed, no trust check). **Literal-only template** with `trustedContext`: `$interpolate('Hello world', false, 'html')(scope)` returns `'Hello world'`. **Invalid context:** `$interpolate('{{x}}', false, 'bogus' as SceContext)` throws. **Interactions:** `mustHaveExpression`, `allOrNothing`, and `::` one-time behaviors still work alongside `trustedContext`. **Pure-ESM wiring:** `createInterpolate({ sceGetTrusted: sce.getTrusted, sceIsEnabled: sce.isEnabled })` enforces trust identically to the DI-resolved `$interpolate`. **[Agent: vitest-testing]**
  - [ ] Regression: re-run existing `src/interpolate/__tests__/*.test.ts` — the spec-011 pass-through path (no `sceGetTrusted`/`sceIsEnabled` callbacks supplied) must still accept `trustedContext` as a no-op. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 7: AngularJS Parity Cross-Reference + Docs + Final Build Verification**
  - [ ] Cross-reference `angular/angular.js/test/ng/sceSpecs.js` and port any scenarios not covered by slices 1–6 into the appropriate existing test file (in particular: `trustAs`-with-already-trusted re-wrap cases, mismatched-context error messages, `parseAs*` scope integration edge cases). **[Agent: vitest-testing]**
  - [ ] Cross-reference `angular/angular.js/test/ng/sceDelegateSpecs.js` and port any scenarios not covered by Slices 2–3 (in particular: resource-URL list edge cases — empty lists, RegExp special cases, pattern escaping corner cases, `'self'` under varied base URLs). **[Agent: vitest-testing]**
  - [ ] Update `CLAUDE.md`: add `./sce` to the Modules table with a one-liner on strict-mode default + `createSce`/`sce` entry points; add a "Non-obvious invariants" bullet ("Strict mode is frozen after config phase — `$sceProvider.enabled(false)` is the only way to disable it"); add a "Where to look when..." row pointing to `src/sce/resource-url-matcher.ts` for URL list logic. **[Agent: typedoc-docs]**
  - [ ] Run `pnpm build` and verify `dist/esm/*`, `dist/cjs/*`, `dist/types/*` generate successfully. Confirm `dist/types/sce/index.d.ts` exposes `createSce`, `sce`, `createSceDelegate`, `sceDelegate`, `SCE_CONTEXTS`, the `Trusted*` classes, `isTrustedValue`, `isTrustedFor`, and the public types. Confirm the updated `dist/types/interpolate/interpolate-types.d.ts` exposes the narrowed `SceContext` parameter type. **[Agent: rollup-build]**
  - [ ] Final verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` all pass. Full regression pass against pre-existing tests (specs 002, 003, 006, 007, 008, 009, 010, 011). **[Agent: typescript-framework]**
