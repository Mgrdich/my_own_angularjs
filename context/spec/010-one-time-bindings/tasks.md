# Tasks: One-Time Bindings (`::` Prefix) & Constant Watch Optimization

- **Specification:** `context/spec/010-one-time-bindings/`
- **Status:** Draft

---

- [x] **Slice 1: Parser — AST Flag Helpers (`isConstant`, `isLiteral`) + Flag Plumbing on Parsed Fn**
  - [x] Create `src/parser/ast-flags.ts` exporting two pure functions: `isConstant(node: ASTNode): boolean` (recursive — only true for `Literal`, `ArrayExpression`/`ObjectExpression`/`PropertyNode` whose children are all constants, plus `UnaryExpression`/`BinaryExpression`/`LogicalExpression`/`ConditionalExpression` whose operands are all constants; short-circuits to false for `Identifier`, `ThisExpression`, `MemberExpression`, `CallExpression`, `AssignmentExpression`) and `isLiteral(program: Program): boolean` (non-recursive — true iff `program.body.type` is `Literal`/`ArrayExpression`/`ObjectExpression`). **[Agent: typescript-framework]**
  - [x] Extend `ExpressionFn` in `src/parser/parse-types.ts` to an intersection type with required readonly `oneTime: boolean`, `constant: boolean`, `literal: boolean` properties. **[Agent: typescript-framework]**
  - [x] In `src/parser/parse.ts`, after building the AST, compute `constant` via `isConstant(ast.body)` and `literal` via `isLiteral(ast)`. Attach all three flags (`oneTime: false` for now, filled in slice 2) to the returned function via `Object.defineProperties` or direct assignment. **[Agent: typescript-framework]**
  - [x] Create `src/parser/__tests__/ast-flags.test.ts` with unit tests for each case in the technical spec §4.2 matrix: primitives (both true), identifier (both false), member access, array of constants vs array with identifiers, object of constants vs object with identifiers, nested literals, function calls, assignment, operator expressions (unary/binary/logical/ternary) over constants. **[Agent: vitest-testing]**
  - [x] Extend `src/parser/__tests__/parse.test.ts` with a new `describe('spec 010 — parse metadata flags', ...)` block asserting `.constant`/`.literal`/`.oneTime` are present and correctly populated on returned fns for sample expressions. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 2: Parser — `::` Prefix Detection + `$eval`/`$apply` Prefix-Stripping Verification**
  - [x] In `src/parser/parse.ts`, trim the input expression as the first step. If the trimmed string begins with `::`, set a local `oneTime = true`, strip the prefix, and validate the remainder is non-empty/non-whitespace (else throw `"Empty expression after '::'"`). Lex/AST-build the stripped remainder. Set `.oneTime` on the returned fn accordingly. **[Agent: typescript-framework]**
  - [x] Add tests under `describe('spec 010 — one-time prefix', ...)` in `parse.test.ts`: (a) `parse('::user.name').oneTime === true` and evaluates identically to `parse('user.name')`, (b) whitespace: `parse('  ::user.name  ')` same as `parse('::user.name')`, (c) sub-expression errors: `parse('a + ::b')`, `parse('::a + ::b')`, `parse('foo(::x)')` all throw, (d) lone `::` throws, (e) `parse('::')`, `parse('::  ')` throw descriptive empty-expression error, (f) non-prefixed expressions have `.oneTime === false`. **[Agent: vitest-testing]**
  - [x] Extend `src/core/__tests__/scope-string-expr.test.ts` with a sub-describe `::  prefix in eval/apply methods`: (a) `scope.$eval('::a + b')` returns same as `scope.$eval('a + b')`, (b) `scope.$apply('::counter = counter + 1')` mutates and digests, (c) `scope.$evalAsync('::x')` queues and runs, (d) `scope.$applyAsync('::y = 1')` coalesces like the non-prefixed form. No watcher-related assertions here (those come in later slices). **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 3: Scope — Constant Watch Optimization (`constantWatchDelegate`)**
  - [x] Create `src/core/scope-watch-delegates.ts` exporting `constantWatchDelegate`. Signature: `(scope: Scope, watchFn: WatchFn, listenerFn: ListenerFn, valueEq: boolean) => DeregisterFn`. Behavior: register via `scope.$watch` with an internal wrapped watch fn that deregisters itself on first invocation (captures the deregister fn returned by `$watch` and calls it inside the first watcher invocation), then returns the underlying value. Match `angular.js:parse.js:1939`. **[Agent: typescript-framework]**
  - [x] In `src/core/scope.ts`, refactor `compileToWatchFn` so that for a string input it preserves the flags from the parsed fn on the returned `WatchFn` (attach `oneTime`/`constant`/`literal` as properties on the wrapper). Function-form inputs return unchanged. **[Agent: typescript-framework]**
  - [x] Modify `$watch` to branch before calling the existing registration path: if the compiled watch fn carries `constant === true`, delegate to `constantWatchDelegate(this, compiled, listenerFn ?? noop, valueEq ?? false)` and return its deregister fn. Otherwise take the existing path. **[Agent: typescript-framework]**
  - [x] Add tests under `describe('spec 010 — constant watch optimization', ...)` in `scope-string-expr.test.ts`: (a) `$watch('42', fn)` fires once then deregisters (subsequent digests do not fire fn), (b) `$watch('"hello"', fn)`, (c) `$watch('[1,2,3]', fn)`, (d) `$watch('::42', fn)` behaves identically to `$watch('42', fn)`, (e) returned deregister fn is safe to call after self-deregistration (no-op), (f) function-form watch expressions behave unchanged (regression). **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 4: Scope — One-Time Watch Delegate (Non-Literal)**
  - [x] In `src/core/scope-watch-delegates.ts`, add `oneTimeWatchDelegate` with the same signature as `constantWatchDelegate`. Behavior: register a wrapping watcher that captures `lastValue = watchFn(scope)` each digest; if `lastValue !== undefined`, schedule `scope.$$postDigest(unwatchIfStillDefined)` where `unwatchIfStillDefined` rechecks and calls the deregister fn only if still defined. Forward the value to the underlying watcher mechanism so the listener fires on change as normal. Mirrors `angular.js:parse.js:1894`. **[Agent: typescript-framework]**
  - [x] In `$watch`, extend the branching: if `constant === false && oneTime === true && literal === false`, delegate to `oneTimeWatchDelegate`. **[Agent: typescript-framework]**
  - [x] Add tests under `describe('spec 010 — one-time bindings (non-literal)', ...)`: (a) `$watch('::user.name', fn)` stays live while `scope.user` is undefined and does NOT fire; once `scope.user.name` becomes non-undefined, fires once and deregisters post-digest, (b) stability treats `null`, `0`, `''`, `false`, `NaN` as stable (i.e., deregisters), (c) after deregistration, further changes to `scope.user.name` do NOT fire the listener, (d) manual deregister before stabilization — listener never fires, (e) never-stabilizing `::` watcher remains live indefinitely (test with many digests), (f) multiple `::` watches on the same scope can coexist and deregister independently. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 5: Scope — One-Time Literal Watch Delegate**
  - [x] In `src/core/scope-watch-delegates.ts`, add `oneTimeLiteralWatchDelegate` with the same signature. Behavior: identical to `oneTimeWatchDelegate` but uses `isAllDefined(value)` as the stability predicate — a helper that returns true iff the value is non-undefined AND (when it's an array/object) all top-level elements/properties are non-undefined. Mirrors `angular.js:parse.js:1931`. **[Agent: typescript-framework]**
  - [x] In `$watch`, extend branching: if `constant === false && oneTime === true && literal === true`, delegate to `oneTimeLiteralWatchDelegate`. **[Agent: typescript-framework]**
  - [x] Add tests under `describe('spec 010 — one-time bindings (literal)', ...)`: (a) `$watch('::[a, b]', fn)` with `scope.a = 1`, `scope.b = undefined` — watcher stays live, fires on each change as a normal watcher would until both are defined, (b) once both `a` and `b` are non-undefined, deregisters post-digest, (c) `$watch('::{x: a, y: b}', fn)` — same rule applied to object literal properties, (d) nested literals: `::[a, [b, c]]` deregisters once `a` and the inner array itself are defined (inner array always defined since constructed each eval), (e) empty literals: `$watch('::[]', fn)` and `$watch('::{}', fn)` deregister immediately on the first digest. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 6: `$watchGroup` Verification — No Code Changes Expected**
  - [x] Verify by reading `$watchGroup` implementation (`src/core/scope.ts:408-453`) that it calls `this.$watch(watchFn, ...)` per entry — each entry's flags flow through the new routing in `$watch` automatically. No code change required. **[Agent: typescript-framework]**
  - [x] Add tests under `describe('spec 010 — $watchGroup with ::', ...)`: (a) `$watchGroup(['::a', 'b'], fn)` — `a` entry deregisters after `scope.a` stabilizes, `b` keeps firing normally; `newValues[0]` retains its last stable value, (b) mixed `['::a', '42', 'b']` — `::a` stabilizes, `'42'` one-shots via constant optimization, `b` continues; listener still fires for `b` changes, (c) all-`::` group `['::a', '::b']` — listener keeps being called until both stabilize, then neither fires again, (d) empty `$watchGroup([], fn)` — regression: listener fires once with empty arrays. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 7: `$watchCollection` — One-Time and Constant Integration**
  - [x] In `src/core/scope.ts`, extend `$watchCollection` to detect when its input `watchFn` is a string. Before calling `compileToWatchFn`, call `parse(watchFn)` once and read `.oneTime` and `.constant`. Store them as local booleans (`isOneTime`, `isConstant`). Still compile to the internal change-counter `WatchFn` as today. **[Agent: typescript-framework]**
  - [x] Modify the internal `$watchCollection` flow: if `isConstant`, wrap the returned deregister fn so it is called in the post-digest immediately after the first listener invocation. If `isOneTime`, wrap `internalListenerFn` so that on the first fire where `newValue !== undefined`, it schedules `this.$$postDigest(() => deregister())`. These wrappings happen inline — no new delegate module needed (per technical spec §2.5). **[Agent: typescript-framework]**
  - [x] Add tests under `describe('spec 010 — $watchCollection with ::', ...)`: (a) `$watchCollection('::items', fn)` — nothing fires while `scope.items` is undefined; once `scope.items = []` (or any array/object), fires once with the snapshot and deregisters post-digest; subsequent mutations do NOT fire, (b) `$watchCollection('items', fn)` (regression) — retains existing collection-mutation behavior indefinitely, (c) `$watchCollection('[1,2,3]', fn)` constant collection — fires once and deregisters, (d) `$watchCollection('::items', fn)` where `items` stays undefined forever — watcher stays live. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 8: AngularJS Parity Cross-Reference + Final Build Verification**
  - [x] Cross-reference `angular/angular.js/test/ng/parseSpec.js` — search for "oneTime", "::", "constant", "literal" test blocks. Identify any parity scenarios not already covered by slices 1–2 tests. Port missing scenarios into `src/parser/__tests__/parse.test.ts`. **[Agent: vitest-testing]**
  - [x] Cross-reference `angular/angular.js/test/ng/rootScopeSpec.js` — search for "oneTime", "::" in `$watch`/`$watchGroup`/`$watchCollection` tests. Identify any scenarios not covered by slices 3–7. Port missing scenarios into `src/core/__tests__/scope-string-expr.test.ts`. **[Agent: vitest-testing]**
  - [x] Run `pnpm build` and verify all dist artifacts (`dist/esm/*`, `dist/cjs/*`, `dist/types/*`) still generate. Confirm `dist/types/parser/parse-types.d.ts` exposes the extended `ExpressionFn` with the three new flag properties. **[Agent: rollup-build]**
  - [x] Final verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` all pass. Full regression pass against pre-existing tests (spec 002, 003, 006, 007, 008, 009). **[Agent: typescript-framework]**
