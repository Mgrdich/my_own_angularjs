# Tasks: `$exceptionHandler` — Centralized Exception Routing

- **Specification:** `context/spec/014-exception-handler/`
- **Status:** Draft

---

- [ ] **Slice 1: Path Alias + Cause Vocabulary Foundation**
  - [ ] Add path alias `@exception-handler/*` → `src/exception-handler/*` in `tsconfig.json`, `vitest.config.ts`, and `rollup.config.mjs` (`tsPathAliases` entry only — the rollup build entry comes in Slice 8). **[Agent: rollup-build]**
  - [ ] Create `src/exception-handler/exception-handler-types.ts` exporting (a) the `ExceptionHandler` callable type `(exception: unknown, cause?: string) => void`, (b) a frozen tuple constant `EXCEPTION_HANDLER_CAUSES = ['watchFn', 'watchListener', '$evalAsync', '$applyAsync', '$$postDigest', 'eventListener', '$digest', '$interpolate'] as const` wrapped in `Object.freeze`, (c) the derived union type `ExceptionHandlerCause = (typeof EXCEPTION_HANDLER_CAUSES)[number]`. Top-of-file TSDoc cites the FS § 2.13 vocabulary lock-in. **[Agent: typescript-framework]**
  - [ ] Create `src/exception-handler/index.ts` re-exporting `ExceptionHandler`, `ExceptionHandlerCause`, `EXCEPTION_HANDLER_CAUSES`. Concrete value exports beyond the const are added in Slice 2. **[Agent: typescript-framework]**
  - [ ] Create `src/exception-handler/__tests__/cause-vocabulary.test.ts` covering: (a) `Object.isFrozen(EXCEPTION_HANDLER_CAUSES) === true`; (b) `EXCEPTION_HANDLER_CAUSES.length === 8` and entries match the eight tokens in declared order; (c) compile-time `satisfies ExceptionHandlerCause` check on each index — locks the union/const drift; (d) attempting to mutate the array (e.g., push, index assignment) throws in strict mode. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 2: ESM Factory — Default Handler + `invokeExceptionHandler`**
  - [ ] Create `src/exception-handler/exception-handler.ts` exporting (a) `consoleErrorExceptionHandler: ExceptionHandler` — calls `console.error('[$exceptionHandler]', exception)` when `cause === undefined`, otherwise `console.error('[$exceptionHandler]', exception, cause)`; (b) `noopExceptionHandler: ExceptionHandler` — a `() => {}` const with TSDoc warning against production use; (c) `exceptionHandler: ExceptionHandler` — default-instance export, equal to `consoleErrorExceptionHandler`; (d) `invokeExceptionHandler(handler: ExceptionHandler, exception: unknown, cause?: string): void` — wraps the call in `try/catch`; on secondary throw, calls `console.error('[$exceptionHandler] handler threw while reporting:', secondary, 'original exception was:', exception)`; never re-invokes the handler on its own throw. All four exports are `const`s (no classes). **[Agent: typescript-framework]**
  - [ ] Add the four new exports to `src/exception-handler/index.ts`. Also extend `src/index.ts` to re-export `invokeExceptionHandler`, `consoleErrorExceptionHandler`, `noopExceptionHandler`, `exceptionHandler`, `EXCEPTION_HANDLER_CAUSES`, plus the `ExceptionHandler` and `ExceptionHandlerCause` types. **[Agent: typescript-framework]**
  - [ ] Create `src/exception-handler/__tests__/exception-handler.test.ts` covering: `consoleErrorExceptionHandler(err)` calls `console.error` exactly once with `'[$exceptionHandler]'` and the error (no trailing `undefined`); `consoleErrorExceptionHandler(err, 'watchFn')` calls `console.error` once with the cause arg; `consoleErrorExceptionHandler('a string')` and `consoleErrorExceptionHandler(null)` do not throw; `noopExceptionHandler(new Error('x'), 'cause')` produces no `console.error` calls and returns `undefined`; `exceptionHandler === consoleErrorExceptionHandler` (default-instance identity); `invokeExceptionHandler(noopExceptionHandler, err, 'cause')` calls the handler once and returns `undefined`; `invokeExceptionHandler(throwingHandler, originalErr, 'cause')` invokes the handler once, calls `console.error` once with the secondary-prefix and BOTH errors, and `invokeExceptionHandler` itself does not throw; the throwing-handler test asserts the handler was invoked exactly once (no recursion). Use `vi.spyOn(console, 'error')` and restore in `afterEach`. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 3: DI Registration on `ngModule`**
  - [ ] Update `src/core/ng-module.ts`: import `consoleErrorExceptionHandler` and the `ExceptionHandler` type from `@exception-handler/index`; extend the `declare module '@di/di-types'` block — add `$exceptionHandler: ExceptionHandler` to the `ng.registry` shape (alphabetically before `$interpolate`); prepend `.factory('$exceptionHandler', () => consoleErrorExceptionHandler)` to the existing `.provider(...)` chain. The `$exceptionHandler` factory has zero DI deps and resolves before any service that consumes it. **[Agent: typescript-framework]**
  - [ ] Create `src/exception-handler/__tests__/di.test.ts` covering: `createInjector([ngModule])` exposes `$exceptionHandler`; `injector.has('$exceptionHandler') === true`; `injector.get('$exceptionHandler') === consoleErrorExceptionHandler`; repeated `injector.get('$exceptionHandler')` returns the same singleton; `config(['$provide', $p => $p.factory('$exceptionHandler', () => mySpy)])` makes `mySpy` the resolved handler; `appModule.factory('$exceptionHandler', () => mySpy)` (registered before `createInjector`) achieves the same override; `appModule.decorator('$exceptionHandler', ['$delegate', $d => (e, c) => { mySpy(e, c); $d(e, c); }])` — wrapper is invoked AND the default `console.error` still runs; `injector.get('$exceptionHandlerProvider')` throws "Unknown provider" (no provider class). **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 4: `Scope` Options + Six Call-Site Rewrites**
  - [ ] Update `src/core/scope-types.ts`: extend `ScopeOptions` with optional `exceptionHandler?: ExceptionHandler` (import the type from `@exception-handler/index`). TSDoc explains the option captures at construction and is stored on root only. **[Agent: typescript-framework]**
  - [ ] Update `src/core/scope.ts`: import `invokeExceptionHandler` and `consoleErrorExceptionHandler` from `@exception-handler/index`; add a `$$exceptionHandler: ExceptionHandler` field to the `Scope` class (initialized to `consoleErrorExceptionHandler` in the constructor, mirroring the `$$ttl = TTL` pattern); update `Scope.create` to read `options?.exceptionHandler ?? consoleErrorExceptionHandler` and assign onto the new root scope; replace each of the six existing `console.error('Error in …', e)` lines with the corresponding `invokeExceptionHandler(this.$root.$$exceptionHandler, e, '<cause>')` call per the FS § 2.13 vocabulary table — `'watchFn'`, `'watchListener'`, `'$evalAsync'`, `'$applyAsync'`, `'$$postDigest'`, `'eventListener'`. The TTL throw site is NOT touched in this slice (deferred to Slice 5). **[Agent: typescript-framework]**
  - [ ] Create `src/core/__tests__/scope-exception-handler.test.ts` covering: `Scope.create()` — `scope.$$exceptionHandler === consoleErrorExceptionHandler`; `Scope.create({ exceptionHandler: spy })` — `scope.$$exceptionHandler === spy`; `Scope.create({ ttl: 5, exceptionHandler: spy })` — both options honored; child scopes (`scope.$new()`) read the handler from `this.$root.$$exceptionHandler` (mutating the spy on root affects child digest behavior); each of the six call sites with a thrown `watchFn` / `watchListener` / `$evalAsync` task / `$applyAsync` task / `$$postDigest` callback / `$on` listener calls the spy once with the correct cause descriptor and the digest / queue / event-loop continues; the watcher's `last` is still updated on a thrown listener (dirty-bookkeeping unaffected); event-listener throw does NOT implicitly stop propagation; recursion guard — a `Scope.create({ exceptionHandler: throwingHandler })` registered with a thrown `watchFn` does NOT crash the digest, `console.error` is called with the secondary-prefix once per dirty pass; backwards compat — `Scope.create()` (no args) with a thrown watcher continues to call `console.error` (the default handler IS console.error). Use `vi.spyOn(console, 'error')` and restore in `afterEach`. **[Agent: vitest-testing]**
  - [ ] Run the existing scope test suite (`pnpm test src/core/__tests__/scope`) and confirm zero regressions — the default handler IS `console.error`, so existing assertions on `console.error` continue to pass unchanged. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 5: Digest TTL Routing**
  - [ ] Update the TTL exhaustion site in `src/core/scope.ts:330`: extract the constructed `Error(...)` into a local `ttlError` variable, call `invokeExceptionHandler(this.$root.$$exceptionHandler, ttlError, '$digest')` BEFORE `throw ttlError`. The `$clearPhase` call ordering is preserved. **[Agent: typescript-framework]**
  - [ ] Extend `src/core/__tests__/scope-exception-handler.test.ts` with TTL-specific tests: `Scope.create({ ttl: 2, exceptionHandler: spy })` plus a watcher that always reports dirty — assert (a) the spy is called exactly once with `cause === '$digest'` and an `Error` instance whose message contains `'iterations reached. Aborting!'` and the last-dirty-watch info, (b) `$apply` re-throws the same `Error` to the test (use `expect(() => scope.$apply(...)).toThrow(...)`), (c) `$$lastDirtyWatch` info is present in the message; recursion-guard variant — TTL with `exceptionHandler: throwingHandler`, `console.error` is called with the secondary-prefix AND the original `ttlError` is still re-thrown (handler failure does NOT mask the TTL signal). **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 6: `$interpolate` ESM Render-Time Catch**
  - [ ] Update `src/interpolate/interpolate-types.ts`: extend `InterpolateOptions` with optional `exceptionHandler?: ExceptionHandler` (import the type from `@exception-handler/index`). TSDoc explains the option captures at factory time and applies to every render-time expression evaluation. **[Agent: typescript-framework]**
  - [ ] Update `src/interpolate/interpolate.ts`: import `invokeExceptionHandler` and `consoleErrorExceptionHandler` from `@exception-handler/index`; resolve `const handler = options.exceptionHandler ?? consoleErrorExceptionHandler` once at factory time; inside the `render` function's per-expression loop, wrap the `fn === undefined ? undefined : fn(context)` evaluation in `try/catch` — on catch, call `invokeExceptionHandler(handler, err, '$interpolate')` and set `value = undefined` so the failed slot funnels into the existing `toInterpolationString(undefined)` branch (renders as `''`). The `allOrNothing` and `oneTime` short-circuits already trigger on `undefined`, so a throw is treated equivalently to no value (per FS § 2.12). Compile-time errors from `parse()` and the spec 012 strict-trust single-binding check continue to throw synchronously at the `$interpolate(text)` call site — they are NOT inside the try/catch. **[Agent: typescript-framework]**
  - [ ] Create `src/interpolate/__tests__/interpolate-exception-handler.test.ts` covering: `createInterpolate({ exceptionHandler: spy })` for `'a {{x.y.z}} b'` (where `x` is undefined, so `x.y.z` throws) — render returns `'a  b'`, spy called once with `cause === '$interpolate'`; multi-expression template `'{{a}} and {{b.c}}'` where only `b` throws — render returns `'A and '`, spy called once; `allOrNothing === true` AND any expression throws — render returns `undefined`, spy still called; `oneTime === true` AND any expression throws — render returns `undefined` for that pass, spy called, the `$watch($interpolate('::a'), …)` integration does NOT deregister on a throw (deregistration still requires non-`undefined` resolution per spec 010); compile-time errors NOT routed — `$interpolate('a {{b +}}')` still throws synchronously at the call site, the handler is NOT called; spec 012 strict-trust compile-time errors NOT routed — `$interpolate('hello {{x}} world', false, 'html')` (surrounding text in trusted context) still throws synchronously, the handler is NOT called; recursion guard — `createInterpolate({ exceptionHandler: throwingHandler })` with a thrown expression does NOT crash the render, `console.error` is called with the secondary-prefix, the failed slot still renders as `''`. **[Agent: vitest-testing]**
  - [ ] Run the existing interpolate test suite (`pnpm test src/interpolate/__tests__/`) and confirm zero regressions. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 7: `$InterpolateProvider` DI Wiring + Cross-Service Integration**
  - [ ] Update `src/interpolate/interpolate-provider.ts`: import `ExceptionHandler` from `@exception-handler/index`; add `'$exceptionHandler'` to the `$get` dep array (after `'$sce'`); update the `$get` factory function to accept the resolved `$exceptionHandler` and forward it as the `exceptionHandler` option to `createInterpolate({ ...existing, exceptionHandler: $exceptionHandler })`. Confirm the dep ordering is correct — `$exceptionHandler` (zero deps) resolves before `$sce` (depends on `$sceDelegate`) which resolves before `$interpolate`. **[Agent: typescript-framework]**
  - [ ] Create `src/exception-handler/__tests__/integration.test.ts` covering end-to-end DI: build an injector with a custom `$exceptionHandler` registered via `$provide.factory` AND `$interpolate` resolved AND a scope created via `Scope.create({ exceptionHandler: injector.get('$exceptionHandler') })` — assert that a thrown watcher AND a thrown interpolation expression BOTH route to the same custom handler with the correct cause descriptors (`'watchFn'` / `'watchListener'` / `'$interpolate'`); decorator pattern — `appModule.decorator('$exceptionHandler', ['$delegate', $d => (e, c) => { mySpy(e, c); $d(e, c); }])` — `mySpy` AND the original `console.error` both fire on a digest error AND on an interpolation error. **[Agent: vitest-testing]**
  - [ ] Run the entire test suite (`pnpm test`) and confirm all prior spec suites (002, 003, 006, 007, 008, 009, 010, 011, 012, 013) still pass unchanged. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 8: Build Output + Docs**
  - [ ] Add the new `exception-handler/index` build entry to `rollup.config.mjs` — mirrors the existing `sce/index` and `sanitize/index` entries (emits `.mjs`, `.cjs`, and `.d.ts`). **[Agent: rollup-build]**
  - [ ] Add `"./exception-handler"` to the `exports` map in `package.json` with the standard `import` / `require` / `types` triplet (mirrors `./sce`, `./sanitize`). **[Agent: rollup-build]**
  - [ ] Run `pnpm build` and verify the new subpath emits `dist/exception-handler/index.mjs`, `dist/exception-handler/index.cjs`, and `dist/exception-handler/index.d.ts`; verify a smoke import `import { exceptionHandler } from 'my-own-angularjs/exception-handler'` resolves the default instance. **[Agent: rollup-build]**
  - [ ] Update `CLAUDE.md`: (a) add `./exception-handler` row to the Modules table listing the seven public exports; (b) add a Non-obvious-invariants bullet stating the digest "log and continue" contract is preserved through `$exceptionHandler`, the default handler is `console.error` (observable behavior unchanged), and the recursion guard degrades to `console.error` on handler-throws; (c) add a "Where to look when…" row "How are runtime errors routed?" → `src/exception-handler/exception-handler.ts` (default + recursion guard), `src/core/scope.ts` (six call sites + TTL), `src/interpolate/interpolate.ts` (render-time catch). **[Agent: typedoc-docs]**
  - [ ] Create `src/exception-handler/README.md` documenting (a) the override pattern (`$provide.factory` and `module.decorator`), (b) the eight cause descriptors and when each fires, (c) the recursion-guard contract — same shape and depth as `src/sanitize/README.md`. **[Agent: typedoc-docs]**
  - [ ] Add TSDoc with at least one usage example to every exported member: `ExceptionHandler` (custom handler example), `consoleErrorExceptionHandler` (default behavior), `noopExceptionHandler` (test-only warning), `exceptionHandler` (default-instance import), `invokeExceptionHandler` (third-party usage example), `EXCEPTION_HANDLER_CAUSES` (iteration / switch-table example), `ExceptionHandlerCause` (type-narrowing example). **[Agent: typedoc-docs]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` must all pass. **[Agent: typescript-framework]**
