# Tasks: Filters — `$filterProvider`, `$filter`, and the Nine Built-ins

- **Specification:** `context/spec/016-filters/`
- **Status:** Draft

---

- [ ] **Slice 1: Pipe Operator End-to-End in the Parser**
  - [ ] Add `'|'` to the `SYMBOLS` set in `src/parser/lexer.ts` (the `Set` declaration around line 32). One-line change. The greedy multi-char ordering at `lexer.ts:108-120` (three-char → two-char → single-char) keeps `||` matching before `|`. **[Agent: typescript-framework]**
  - [ ] Extend `src/parser/parse-types.ts`: add `interface FilterExpression { readonly type: 'FilterExpression'; readonly input: ASTNode; readonly name: string; readonly arguments: ASTNode[] }` and include it in the `ASTNode` discriminated union. `name` is a string (not an `Identifier` node) — filter names are syntactically distinct from scope identifiers. **[Agent: typescript-framework]**
  - [ ] Add the `filterChain` production to `src/parser/ast.ts`: a new function `filterChain(): ASTNode` that reads a `ternary()` into `left`, then while `expect('|') !== undefined` reads an `identifier()` for the filter name, then loops on `expect(':')` consuming `assignment()` for each filter argument, wrapping into `{ type: 'FilterExpression', input: left, name: nameToken.name, arguments: args }`. Update `assignment()` to call `filterChain()` instead of `ternary()` for its left operand. The existing l-value check inside `assignment()` (`left.type !== 'Identifier' && left.type !== 'MemberExpression'`) automatically rejects `a | f = b` with the existing `'Trying to assign a value to a non l-value'` message — no new throw needed. **[Agent: typescript-framework]**
  - [ ] Extend `src/parser/ast-flags.ts` `isConstant`: add a `case 'FilterExpression'` that returns `isConstant(node.input) && node.arguments.every(isConstant)` — note this is the parse-time check; the runtime `$stateful` re-check lives in scope (Slice 11). For the immediate slice, ALSO short-circuit constness conservatively: `parse(expr)` should NOT mark a filter-containing expression as `constant: true` because the filter function is unknown at parse time. The simplest implementation: change `parse.ts` to wrap `isConstant(ast.body)` with an additional `&& !containsFilterExpression(ast.body)` walk. Add `containsFilterExpression(node: ASTNode): boolean` to `ast-flags.ts` for that check. The `case 'FilterExpression'` in `isConstant` is still needed for the watch-install upgrade path in Slice 11. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/filter-error.ts` exporting `class FilterLookupError extends Error { readonly name = 'FilterLookupError' as const; constructor(filterName: string) { super(`Unknown filter: ${filterName}`); } }`. Create `src/filter/index.ts` re-exporting `FilterLookupError`. Add the `@filter/*` path alias to `tsconfig.json`. The path alias will be exercised in Slice 2; for now it just needs to resolve. **[Agent: typescript-framework]**
  - [ ] Extend `src/parser/interpreter.ts` `evaluate()` with a new `case 'FilterExpression'` branch: read `locals?.$$filter` and cast to `((name: string) => FilterFn) | undefined`. If absent, throw `new FilterLookupError(node.name)` (this branch covers the "outside-injector context" case from FS §2.8). If present, call it to resolve the filter — propagating any thrown `FilterLookupError`; evaluate `node.input` and `node.arguments` against the same scope/locals; call the resolved filter as `filterFn(inputValue, ...argValues)`. Reserve the `$$filter` name as part of the implicit "internal locals" surface (no public docs change in this slice; documented in Slice 13). **[Agent: typescript-framework]**
  - [ ] Add type `FilterFn = ((value: unknown, ...args: unknown[]) => unknown) & { $stateful?: boolean }` to `src/filter/filter-types.ts`; re-export from `src/filter/index.ts`. The interpreter imports `FilterFn` for the local cast above. **[Agent: typescript-framework]**
  - [ ] Create `src/parser/__tests__/lexer-pipe.test.ts`: assert `lex('a||b')` yields a single `||` token (existing logical-OR contract preserved); `lex('a|b')` yields three tokens with text `'|'` for the middle one; `lex('a|b|c')` yields `[a, |, b, |, c]`; whitespace tolerance `lex('a | b')` matches `lex('a|b')` after stripping whitespace tokens. **[Agent: vitest-testing]**
  - [ ] Create `src/parser/__tests__/ast-filter.test.ts` covering FS §2.1: AST shape for `parse('value | uppercase')`, chaining left-to-right `parse('a | f1 | f2')`, argument collection `parse('value | filterName : arg1 : arg2')`, full-expression args `parse('items | limitTo : count + 1 : start')`, precedence vs `+` (`a + b | uppercase` → filter wraps the BinaryExpression), precedence vs `?:` (`a ? b : c | uppercase` → filter wraps the entire ternary), assignment of filtered value (`a = b | f` parses as `a = (b | f)`), parse error on `a | f = b` (assert message contains `'non l-value'`), `||` regression (`a || b` still produces `LogicalExpression`), whitespace tolerance. **[Agent: vitest-testing]**
  - [ ] Create `src/parser/__tests__/interpreter-filter.test.ts`: `parse('x | upper')(scope, { $$filter: name => upperFn })` evaluates correctly when `upperFn = (s) => String(s).toUpperCase()`; throws `FilterLookupError` (instance check) when `$$filter` is absent or returns no match; argument expressions evaluate against scope (`parse('items | limitTo : count')(scope, { $$filter, count: 3 })`); `this`-binding preserved in arg eval. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test` — all must pass. Every test from prior specs (002, 003, 006, 007, 008, 009, 010, 011, 012, 013, 014, 015) continues to pass unchanged. The only behavior change for non-filter expressions is the lexer's new `|` token, which is unreachable from any prior expression (none used a single `|`). **[Agent: typescript-framework]**

- [ ] **Slice 2: `$filterProvider` and `$filter` Service in DI**
  - [ ] Create `src/filter/filter-types.ts` (extending Slice 1's stub) with public types: `FilterFactory = Invokable<FilterFn>`, `FilterService = (name: string) => FilterFn`, `interface IFilterProvider { register(name: string, factory: FilterFactory): this; register(map: Record<string, FilterFactory>): this }`. Re-export from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/filter-service.ts` exporting `function createFilter($injector: Injector, registrations: ReadonlyMap<string, FilterFactory>): FilterService`. Implementation: maintain a per-call-time `Map<string, FilterFn>` cache; on first call for a given name, look up the registration, call `$injector.invoke(factory)`, cache and return; on cache hit, return the cached function; on registration miss, throw `new FilterLookupError(name)`. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/filter-provider.ts` exporting `class $FilterProvider`. Fields: private `$$registrations = new Map<string, FilterFactory>()`. Methods: overloaded `register(nameOrMap, factory?)` per FS §2.2 — string form validates `name` is a non-empty string with no whitespace (rejects with a descriptive error), stores the factory; object form iterates `Object.entries(map)` and recurses into the string form. Returns `this` for chaining. The `$get` array-style invokable declares `'$injector'` as a dep and calls `createFilter($injector, this.$$registrations)`. Mirror the `$SceProvider` shape (`src/sce/sce-provider.ts`). **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: add `.provider('$filter', $FilterProvider)` to the `ngModule` chain. Add the typed-registry augmentation entries: `$filter: FilterService` in the `registry` block; `$filterProvider: $FilterProvider` in the `config` block. Import `$FilterProvider` from `@filter/filter-provider` and types from `@filter/filter-types`. **[Agent: typescript-framework]**
  - [ ] Add the new entry `{ name: 'filter/index', input: 'src/filter/index.ts' }` to `entries` in `rollup.config.mjs`. Add `'@filter/*': ['src/filter/*']` to `tsPathAliases` in the same file. **[Agent: rollup-build]**
  - [ ] Add `"./filter"` entry to the `exports` map in `package.json`, mapping to the built `.mjs`/`.cjs`/`.d.ts` outputs that Rollup emits. Mirror the existing `"./sce"` / `"./sanitize"` shapes. **[Agent: rollup-build]**
  - [ ] Create `src/filter/__tests__/filter-provider.test.ts` covering FS §2.2: register accepts string + factory pair; array-style annotations resolve through `$injector.invoke`; chaining via return-this; object form `register({ a: …, b: … })` registers each key; last-wins re-registration replaces the prior factory; `$filterProvider` is resolvable in any module's `config()` block; out-of-phase post-run-phase call throws (inherits the spec-015 phase guard via the underlying `$provide.provider` machinery — assert via direct provider use). **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/filter-injectable.test.ts` covering FS §2.3: `injector.get('$filter')` returns a function; calling `$filter('foo')` for an unregistered name throws `FilterLookupError` with message `Unknown filter: foo` and instanceof check; identity stability (`$filter('foo') === $filter('foo')` once a `foo` filter is registered); `$filter` injectable into a `module.factory` consumer; absence in config blocks (config block requesting `'$filter'` throws `Unknown provider: $filter` because the run-phase service is not config-resolvable). **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, AND `pnpm build` (the new `./filter` entry must compile cleanly). All must pass. **[Agent: rollup-build]**

- [ ] **Slice 3: Module DSL `.filter()` and the `<name>Filter` Provider Shim**
  - [ ] Add `.filter<K extends string, F extends FilterFn>(name: K, factory: Invokable<F>)` to the `Module` class in `src/di/module.ts`. Implementation: pushes two coordinated records onto `$$invokeQueue` per technical-considerations §2.8 — (a) a `provider` recipe under name `\`${name}Filter\`` whose source is an object literal with `$get = ['$filter', ($filter: FilterService) => $filter(name)] as const`; (b) a `config` block invokable `['$filterProvider', ($fp: $FilterProvider) => $fp.register(name, factory)]`. The `config` push uses the existing `.config(...)` queueing path (whichever invoke-queue tag the existing `.config` method uses). Type-widen the return so the chain accumulates `${K}Filter` in the registry (mirrors the `.provider` widening pattern). The `as never` cast on the return is acceptable per the same precedent spec 015 used. **[Agent: typescript-framework]**
  - [ ] Type-augmentation: extend `src/di/di-types.ts` if necessary so `Module<…>.filter<…>(…)` correctly accumulates `${K}Filter: F` into the typed registry. If the existing `.provider`-widening machinery covers it, no change needed; otherwise add a minimal helper type. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/module-dsl.test.ts` covering FS §2.4: `createModule('app', ['ng']).filter('shout', ['<factory>'])` makes `$filter('shout')(...)` work AND `injector.get('shoutFilter')` return the SAME reference; `.filter` returns `this` for chaining; array-style annotations resolve dependencies; last-wins within a single chain (`.filter('shout', oldF).filter('shout', newF)`); cross-module last-wins (parent module DSL .filter, child module's config block does `$filterProvider.register(...)` — child wins); shared-registry assertion (`.filter` writes into the same map every other writer touches — verifiable by reading via `$filter` after a `.filter` write). **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/provider-name-convention.test.ts` covering FS §2.5: `injector.get('shoutFilter') === $filter('shout')`; `injector.has('shoutFilter') === true` and `injector.has('$filter') === true`; filter name ending in `Filter` (`module.filter('myFilter', …)`) registers provider as `myFilterFilter` (no special-case stripping); invalid identifier (e.g., name with spaces) throws at registration time. **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/decorator.test.ts` covering FS §2.6: register a custom filter via `.filter('shout', ...)`; decorate it via `module.decorator('shoutFilter', ['$delegate', ($d: FilterFn) => (s: unknown) => `[${$d(s)}]`])`; `$filter('shout')('hi') === '[hi!]'`. Repeat with `$provide.decorator(...)` from a config block. Multi-decorator stacking (`d1` then `d2` produces `d2(d1(orig))`). Decorator on a non-existent filter (`module.decorator('nonexistentFilter', …)`) — `$filter('nonexistent')` throws `Unknown filter`; `injector.get('nonexistentFilter')` throws `Unknown provider`. TypeScript-level assertion: the `$delegate` parameter of a decorator on `'shoutFilter'` is typed `FilterFn` (use a `expectType`-style helper or compile-time type-narrowing assertion). **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: typescript-framework]**

- [ ] **Slice 4: Scope and `$exceptionHandler` Wiring for Filter Expressions**
  - [ ] Extend `EXCEPTION_HANDLER_CAUSES` in `src/exception-handler/exception-handler-types.ts`: append `'$filter'` as the ninth tuple element. The derived `ExceptionHandlerCause` union picks it up automatically. Update the docstring comment listing the tokens. Update the example `case '$filter':` row in the docstring's switch sample. **[Agent: typescript-framework]**
  - [ ] Update `src/core/scope.ts`: extend the `Scope.create` options bag with optional `filterLookup?: FilterService`. Stash on the scope (private field, e.g. `$$filterLookup`). Inside the watch-evaluation site of `$digest` (where `watchFn(scope, locals)` is currently called), build a fresh locals object that merges in `{ $$filter: this.$$filterLookup }` ONLY IF `$$filterLookup` is defined. Existing scopes constructed without the option continue to work — expressions without filters are unaffected; expressions WITH filters throw `Unknown filter:` because the lookup is missing. **[Agent: typescript-framework]**
  - [ ] In the same `$digest` catch site, refine the cause selection: when `err instanceof FilterLookupError`, pass `'$filter'` to `invokeExceptionHandler` instead of the existing `'watchFn'` / `'watchListener'` token. Repeat for the `$evalAsync` / `$applyAsync` / `$$postDigest` catch sites if they evaluate parsed expressions through filters (reasoning: any path that runs a filter expression must surface filter-lookup errors with the right cause). Verify the existing eight cause-token tests in `src/core/__tests__/` still pass — none of them route filter-lookup errors today, so the new branch is additive. **[Agent: typescript-framework]**
  - [ ] Update `src/interpolate/interpolate.ts`: in the render-time catch site (the one currently routing through `'$interpolate'` cause), add an instanceof check — `err instanceof FilterLookupError` routes with `'$filter'` cause instead. Plumb `$filter` through `$interpolate` similarly to scope: `$InterpolateProvider.$get` already has injector access; pass `$filter` (or the `$$filter` lookup) into the interpolation closure so per-segment expression evaluation can supply it via locals. **[Agent: typescript-framework]**
  - [ ] Wire the `ngModule`'s eventual `$rootScope` factory to pass `filterLookup: $filter` into `Scope.create` — DEFERRED to the bootstrap roadmap item (when `$rootScope` lands as a registered factory). For this slice, scope's option is exercised by tests directly: `Scope.create({ filterLookup: ($filter as FilterService) })`. Add an inline TODO comment in `ngModule` near the location where `$rootScope` will eventually live, citing this spec's slice. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/exception-handler-integration.test.ts` covering FS §2.8: `parse('x | nonexistent')` succeeds (no throw); calling the parsed expression inside `$digest` with a typo'd filter routes through `$exceptionHandler` with cause `'$filter'` and the digest continues; sibling watches in the same cycle still run; `$filter('nonexistent')` direct call throws synchronously (not via handler); `EXCEPTION_HANDLER_CAUSES.includes('$filter') === true`; the typed `ExceptionHandlerCause` union now includes `'$filter'` (compile-time assertion). **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/scope-watch-integration.test.ts`: register a custom filter via `module.filter('shout', () => (s) => `${s}!`)`; build the injector; create a child scope with `Scope.create({ filterLookup: $filter })`; `scope.$watch('msg | shout', listener)`; mutate `scope.msg = 'hi'`; run `$digest`; assert the listener received `'hi!'`. Add a chain-of-filters watch (`'msg | shout | uppercase'`) and assert the right composition (note: requires `uppercase` from Slice 5 — either gate this assertion behind Slice 5's landing OR use two custom filters here). **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. Special check: `EXCEPTION_HANDLER_CAUSES` length is now 9 (was 8) — any test asserting the literal length needs an update. Grep for `.length === 8` and `EXCEPTION_HANDLER_CAUSES` co-occurrences in `src/**/__tests__/`. **[Agent: vitest-testing]**

- [ ] **Slice 5: Trivial Built-ins — `uppercase`, `lowercase`, `json`**
  - [ ] Create `src/filter/case.ts` exporting `uppercaseFilterFactory: () => FilterFn` and `lowercaseFilterFactory: () => FilterFn`. Implementations: `(input) => typeof input === 'string' ? input.toUpperCase() : input` and the symmetric lower case. No DI deps (factory takes no arguments). **[Agent: typescript-framework]**
  - [ ] Create `src/filter/json.ts` exporting `jsonFilterFactory: () => FilterFn`. Implementation: `(input, spacing) => JSON.stringify(input, null, typeof spacing === 'number' ? spacing : 2)`. Returns `undefined` for `undefined` input (matches `JSON.stringify(undefined)`). **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: register the three filters via `.filter('uppercase', uppercaseFilterFactory).filter('lowercase', lowercaseFilterFactory).filter('json', jsonFilterFactory)`. (Use the new `.filter` DSL from Slice 3 — one chained call per filter.) Add the typed-registry entries: `uppercaseFilter: FilterFn`, `lowercaseFilter: FilterFn`, `jsonFilter: FilterFn`. **[Agent: typescript-framework]**
  - [ ] Re-export `uppercaseFilterFactory`, `lowercaseFilterFactory`, `jsonFilterFactory` from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/case.test.ts` covering FS §§2.17, 2.18: each acceptance criterion as one assertion. Use the canonical injector setup pattern (the same `createInjector(['ng'])` boilerplate other tests in the project use) so the test exercises the full chain: ngModule → `.filter` DSL → `$filterProvider` → `$filter` lookup. **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/json.test.ts` covering FS §2.19: each acceptance criterion as one assertion. **[Agent: vitest-testing]**
  - [ ] Add an end-to-end interpolation test: `$interpolate('Hello {{ name | uppercase }}!')(scope)` where `scope.name = 'world'` returns `'Hello WORLD!'`. This exercises the cross-module integration (ngModule → $filter → interpreter → $interpolate). Place in `src/filter/__tests__/interpolate-integration.test.ts`. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: typescript-framework]**

- [ ] **Slice 6: `limitTo` Built-in**
  - [ ] Create `src/filter/limit-to.ts` exporting `limitToFilterFactory: () => FilterFn`. Implementation per FS §2.13: dispatch on `Array.isArray(input)` → array slicing, `typeof input === 'string'` → string slicing, `typeof input === 'number'` → coerce to string and recurse. Positive limit slices from `begin` (default 0) for `limit` items; negative limit slices the LAST `|limit|` items. `Infinity` returns the full input. Non-handled input types return input unchanged. Returns a new array/string — never mutates. **[Agent: typescript-framework]**
  - [ ] Re-export `limitToFilterFactory` from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: add `.filter('limitTo', limitToFilterFactory)` to the ngModule chain. Update the typed registry. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/limit-to.test.ts` covering FS §2.13's nine acceptance criteria. Include the `Infinity` and negative-limit boundary cases. Verify input non-mutation by passing a frozen array and asserting no `TypeError`. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: typescript-framework]**

- [ ] **Slice 7: `$locale` Provider + `formatNumber` Helper + `currency` and `number` Built-ins**
  - [ ] Create `src/filter/locale-types.ts` with the typed interfaces: `LocaleService`, `NumberFormats`, `NumberPattern`, `DatetimeFormats`. Field types per technical-considerations §2.10. Re-export `LocaleService` from `src/filter/index.ts`; the sub-types stay internal. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/locale.ts` exporting `defaultLocale: LocaleService` as a frozen literal with the en-US values per technical-considerations §2.10 — including the two `PATTERNS` (number + currency), DAY/SHORTDAY/MONTH/SHORTMONTH/AMPMS arrays, and the named date formats (`medium`, `short`, `fullDate`, `longDate`, `mediumDate`, `shortDate`, `mediumTime`, `shortTime`). Use `Object.freeze` recursively (top-level + each sub-object) so accidental mutation throws in strict mode. Re-export from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: add `.factory('$locale', () => defaultLocale)` to the ngModule chain. Add `$locale: LocaleService` to the typed registry. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/format-number.ts` (internal helper, NOT re-exported from `index.ts`) exporting `formatNumber(value: number, pattern: NumberPattern, fractionSize: number, locale: LocaleService): string`. Implementation: validate `value` is finite; round to `fractionSize` decimal digits using standard half-away-from-zero (matches AngularJS); split into integer + fractional parts; apply group separator (`locale.NUMBER_FORMATS.GROUP_SEP`) at `pattern.gSize` / `pattern.lgSize` boundaries; apply decimal separator (`locale.NUMBER_FORMATS.DECIMAL_SEP`); prepend negative-prefix / append negative-suffix for negatives. Return `Infinity`/`NaN` early as `'∞'` / `'-∞'` / `''` per FS §2.15. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/currency.ts` exporting `currencyFilterFactory: ['$locale', ($locale: LocaleService) => CurrencyFilterFn]` (array-style annotation). Implementation per FS §2.14: `(amount, symbol, fractionSize) => …` — return `''` for non-numeric input; default `symbol` to `$locale.NUMBER_FORMATS.CURRENCY_SYM`; default `fractionSize` to `2`; format via `formatNumber(amount, $locale.NUMBER_FORMATS.PATTERNS[1], fractionSize, $locale)`; substitute the `¤` placeholder in pattern's prefix/suffix with the resolved symbol. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/number.ts` exporting `numberFilterFactory: ['$locale', ($locale: LocaleService) => NumberFilterFn]`. Implementation per FS §2.15: `(value, fractionSize) => …` — return `''` for non-numeric / NaN; handle `Infinity`/`-Infinity` early; format via `formatNumber(value, $locale.NUMBER_FORMATS.PATTERNS[0], fractionSize, $locale)`. When `fractionSize` is `undefined`, use the pattern's `maxFrac` for max digits but trim trailing zeros (FS §2.15 acceptance: `1.5 | number` → `'1.5'`, NOT `'1.500'`). **[Agent: typescript-framework]**
  - [ ] Re-export `currencyFilterFactory` and `numberFilterFactory` from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: add `.filter('currency', currencyFilterFactory).filter('number', numberFilterFactory)`. Update typed registry. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/locale.test.ts` covering FS §2.20 acceptance criteria: `injector.get('$locale').id === 'en-us'`; pattern shapes match; named formats present; swap via `module.factory('$locale', ...)` propagates to `currency` filter output. **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/currency.test.ts` covering FS §2.14's nine acceptance criteria. Include the negative-pattern parentheses behavior (`-1234.5 | currency:'$'` → `'($1,234.50)'`) per the en-US pattern. **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/number.test.ts` covering FS §2.15's ten acceptance criteria. Include the trailing-zero-trim test, the `Infinity` / `NaN` handling, and the `1e21`-large-number edge case. **[Agent: vitest-testing]**
  - [ ] Add a focused unit test for `formatNumber` under `src/filter/__tests__/format-number.test.ts` — small but isolates the rounding + grouping logic from `$locale` plumbing. Table-driven with ~15 cases. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: typescript-framework]**

- [ ] **Slice 8: `formatDate` Helper + `date` Built-in**
  - [ ] Create `src/filter/format-date.ts` (internal helper, NOT re-exported) exporting `formatDate(input: Date | number | string, format: string, timezone: string | undefined, locale: LocaleService): string`. Implementation per FS §2.16: parse `input` into a `Date` (Date instance pass-through, ms-number via `new Date(n)`, ISO-8601 string via `Date.parse`); resolve `format` against `locale.DATETIME_FORMATS` named formats first (returns the underlying token string); apply `timezone` shift to the date (UTC offset adjustment when `timezone === 'UTC'`); scan the format string for tokens (longest-first match against the static token table) interleaved with literal text; single-quote-escaped runs (`'literal'`) emit literal text; emit each token's substituted output. The token table is a `Map<string, (date: Date, locale: LocaleService, timezone?: string) => string>` keyed by token text — `yyyy`, `yy`, `y`, `MMMM`, `MMM`, `MM`, `M`, `LLLL`, `dd`, `d`, `EEEE`, `EEE`, `HH`, `H`, `hh`, `h`, `mm`, `m`, `ss`, `s`, `sss`, `.sss`, `a`, `Z`, `ZZ`, `ww`, `w`. Returns input unchanged for non-parseable strings; returns `''` for `null`/`undefined`. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/date.ts` exporting `dateFilterFactory: ['$locale', ($locale: LocaleService) => DateFilterFn]`. The filter is a thin call to `formatDate(input, format ?? 'mediumDate', timezone, $locale)`. **[Agent: typescript-framework]**
  - [ ] Re-export `dateFilterFactory` from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: add `.filter('date', dateFilterFactory)`. Update typed registry. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/date.test.ts` covering FS §2.16's seventeen acceptance criteria. Pin the system timezone via `vi.setSystemTime(new Date('2026-05-07T14:30:45Z'))` and assert in UTC for absolute-time checks; for local-time tokens, freeze a Date with explicit local-time components and assert against those. The token table gets at least one assertion per token; named formats (`'medium'`, `'short'`, etc.) get one assertion each. **[Agent: vitest-testing]**
  - [ ] Add a focused unit test for `formatDate` under `src/filter/__tests__/format-date.test.ts` — table-driven, ~25 cases — covering the longest-first token-match priority, the single-quote-escape rule, and the named-format → token-string indirection. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: typescript-framework]**

- [ ] **Slice 9: `filter` Built-in (Array Filtering)**
  - [ ] Create `src/filter/filter-filter.ts` exporting `filterFilterFactory: () => FilterFn`. Implementation per FS §2.11: dispatch on `expression` type — string (substring case-insensitive match against any string prop), object (per-property substring match; `$` key matches any property; nested objects recurse), function (predicate). `comparator` argument: `true` → strict equality; function → custom comparator; otherwise → default substring matcher. `anyPropertyKey` argument (default `'$'`) — replaces `$` as the wildcard key. Returns input unchanged when `expression` is `undefined` / `null` / `''`. Non-array input returns input unchanged. Always returns a NEW array — never mutates. **[Agent: typescript-framework]**
  - [ ] Re-export `filterFilterFactory` from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: add `.filter('filter', filterFilterFactory)`. Update typed registry. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/filter-filter.test.ts` covering FS §2.11's eleven acceptance criteria. Include a circular-input regression test (input array with a back-reference) — the filter must NOT recurse into the input when the comparator is `true` or a function (recursion is bounded by the EXPRESSION's structure, not the input's). Use AngularJS port test vectors from `angular/angular.js/test/ng/filter/filterSpec.js` and mark them with comments. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: typescript-framework]**

- [ ] **Slice 10: `orderBy` Built-in**
  - [ ] Create `src/filter/order-by.ts` exporting `orderByFilterFactory: () => FilterFn`. Implementation per FS §2.12: predicate normalization step turns string (`'name'`, `'-name'`, `'+name'`) / function / array-of-mixed into a unified `[{ getter, reverse }, ...]` shape. Comparison helper handles AngularJS's type-precedence rules (numbers numerically, strings via `localeCompare`-like semantics, `null`/`undefined` to the end, mixed types by `typeof` precedence). Stable sort via decorate-sort-undecorate (attach original index to each item, fall back to it on tie). Returns a new array — never mutates. Non-array input returns input unchanged. The `reverse` argument flips the entire order; the `comparator` argument replaces the default comparison. The empty predicate `'+'` sorts by item identity (`x => x`). **[Agent: typescript-framework]**
  - [ ] Re-export `orderByFilterFactory` from `src/filter/index.ts`. **[Agent: typescript-framework]**
  - [ ] Update `src/core/ng-module.ts`: add `.filter('orderBy', orderByFilterFactory)`. Update typed registry. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/order-by.test.ts` covering FS §2.12's thirteen acceptance criteria. Include the stable-sort assertion (items with equal sort keys retain their relative order — assert via a sentinel index). Port test vectors from `angular/angular.js/test/ng/filter/orderBySpec.js`. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: typescript-framework]**

- [ ] **Slice 11: One-Time / Constant Watch Delegate Re-Check + Stateful Filter Handling**
  - [ ] Add `containsStatefulFilter(node: ASTNode, $filter: FilterService): boolean` to `src/parser/ast-flags.ts`. Pure tree walk: returns `true` if any `FilterExpression` node's resolved filter has `$stateful === true`. The `$filter` parameter is the runtime lookup function. **[Agent: typescript-framework]**
  - [ ] Update `src/core/scope.ts` watch-installation logic: when a parsed expression has `oneTime: true` AND contains a `FilterExpression`, run a re-check at install time. The re-check: (a) walk via `containsStatefulFilter(ast, $filter)` — if any stateful filter is found, treat the watch as a regular watcher (NOT one-time, NOT constant); (b) otherwise call `isConstant(ast.body)` (the existing helper) — if true, route through `constantWatchDelegate` / `oneTimeLiteralWatchDelegate` per spec 010's selection rules. The watch-install path needs access to the AST; if the parsed `ExpressionFn` doesn't currently expose the AST, add a private/symbolic `$$ast` property on the function (alongside `oneTime`/`constant`/`literal`) that the parser sets before returning. **[Agent: typescript-framework]**
  - [ ] Update `src/parser/parse.ts`: attach the AST root onto the returned function under a non-enumerable `$$ast` property, mirroring the existing `Object.defineProperties` block that sets `oneTime`/`constant`/`literal`. The property is intentionally `$$`-prefixed (internal) and `enumerable: false`. **[Agent: typescript-framework]**
  - [ ] Create `src/filter/__tests__/stateful.test.ts` covering FS §2.7's five acceptance criteria: default-stateless behavior (filter is not re-run when input is identity-stable across digests); `$stateful: true` opt-in (a filter returning `Date.now()` produces changing values across digests with stable input); all nine built-ins are stateless (loop through them and assert `$filter(name).$stateful !== true`); `$stateful` is on the filter function (not the factory) — assert via `Object.getOwnPropertyDescriptor`; one-time / constant downgrade (a `::expr | statefulFilter` watch is treated as a regular watcher, not one-time). **[Agent: vitest-testing]**
  - [ ] Create `src/filter/__tests__/one-time-binding.test.ts` covering FS §2.9's four acceptance criteria. Use the existing spec-010 one-time-binding test infrastructure as a model. Verify the upgrade path: `'::value | uppercase'` (constant input + stateless filter) deregisters at the first digest after the input stabilizes; `'::items | orderBy:"name"'` deregisters once the orderBy output is defined; a stateful filter downgrades the `::` to a normal watcher. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: vitest-testing]**

- [ ] **Slice 12: SCE / `$interpolate` Cross-Module Integration Tests**
  - [ ] Create `src/filter/__tests__/sce-interaction.test.ts` covering FS §2.10's four acceptance criteria. Set up: ngModule + ngSanitize loaded, custom `markup` scope value. Assertions: (a) `$interpolate('{{ markup | uppercase }}', false, { trustedContext: SCE_CONTEXTS.HTML })(scope)` runs uppercase, then $sce.getTrustedHtml, then $sanitize; (b) same expression with ngSanitize NOT loaded and a plain string for `markup` — throws `unsafe value used in HTML context` per spec-012 strict mode; (c) a filter that wraps via `$sce.trustAsHtml` produces output that survives through `$sce.getTrustedHtml` (the wrapper unwraps directly without calling $sanitize); (d) the `{{expr}}` single-binding rule from spec 011 (`strictTrustActive`) still fires for filter-containing expressions inside multi-segment interpolations with a trusted context. NO source code changes in this slice — just test verification of behavior that already composes naturally. **[Agent: vitest-testing]**
  - [ ] Add an integration smoke test in `src/filter/__tests__/integration.test.ts`: build an injector with `ngModule + ngSanitize + appModule`; appModule uses `.filter('reverse', ...)` to register a custom filter; `$interpolate` an expression `{{ message | reverse | uppercase }}` against a scope; assert correct end-to-end output. Confirms the runtime composition of multiple cross-module pieces works. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`. All must pass. **[Agent: vitest-testing]**

- [ ] **Slice 13: Documentation**
  - [ ] Update `CLAUDE.md` per FS §2.23: (a) add a new "Modules" table row for `./filter` listing the public exports — `createFilter`, `$FilterProvider`, `$filter` (default factory), `$locale`, `defaultLocale`, the nine built-in factories, `FilterFn`, `FilterFactory`, `LocaleService`, `FilterLookupError`; (b) add a "Non-obvious invariants" bullet covering: filters internally registered as `<name>Filter` providers; `$stateful = true` on the filter function (not the factory) opts out of digest fast-path; `$locale` swap is a single-factory replacement; unknown-filter routes through `$exceptionHandler` cause `'$filter'` at digest time; `parse(expr)` marks any filter-containing expression as `constant: false` at parse time and re-checks at watch install once `$filter` is resolvable; (c) add "Where to look when…" rows for "How is `<expr> | filter` parsed?" → `src/parser/ast.ts` (filterChain production) + `src/parser/interpreter.ts` (FilterExpression case); "How are filters registered from a module?" → `src/filter/filter-provider.ts` + `src/di/module.ts` (`.filter` method); "How does the `date` filter format tokens?" → `src/filter/format-date.ts`. **[Agent: typedoc-docs]**
  - [ ] Create `src/filter/README.md` per FS §2.23: en-US-default-only stance; swap pattern for non-English locales (`module.factory('$locale', () => …)` with a small example); `$stateful` flag (when to use it, why all built-ins are stateless); custom-filter authoring guide (factory shape, array-style annotations, return types). **[Agent: typedoc-docs]**
  - [ ] Add TSDoc comments with at least one usage example to each public export per FS §2.23: the nine built-in filter factories, `$FilterProvider.register`, `$filter` (the service), `$locale`, `module.filter`, `FilterFn`, `FilterFactory`, `LocaleService`, `FilterLookupError`, `defaultLocale`, `createFilter`. Each docstring follows the project convention used in `src/sce/sce-provider.ts` and `src/exception-handler/exception-handler.ts`. **[Agent: typedoc-docs]**
  - [ ] Update the `src/filter/index.ts` barrel docstring (top-of-file comment) describing the module's purpose and the dual surface (ESM-first factories vs. DI-layer registration via `ngModule`). Mirror the docstring style of `src/sce/index.ts` and `src/sanitize/index.ts`. **[Agent: typedoc-docs]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, AND `pnpm build`. All must pass. The build's TypeDoc-style declaration emit must include the documented examples in the `.d.ts` files (verify by spot-checking `dist/types/filter/index.d.ts`). **[Agent: typedoc-docs]**
  - [ ] Run `/awos:verify` against this spec — every acceptance-criterion checkbox in `functional-spec.md` should flip from `[ ]` to `[x]`. Any criterion still marked `[ ] NOT MET` requires a follow-up. **[Agent: vitest-testing]**

---

## Notes on Subagent Assignments

| Slice / Task | Agent | Rationale |
| --- | --- | --- |
| Source code changes touching parser, scope, DI, interpolate, exception-handler | `typescript-framework` | Core framework module changes per the agent's description. |
| Test file authoring + test-suite verification | `vitest-testing` | Vitest unit tests including jsdom-driven integration. |
| Build / Rollup / package.json / path-alias changes | `rollup-build` | Build pipeline configuration per the agent's description. |
| TSDoc, CLAUDE.md, README, declaration-emit verification | `typedoc-docs` | Documentation generation and project-level docs per the agent's description. |

No tasks are assigned to `general-purpose` — every step has a clear specialist match.

No new MCPs or external services are required. Tests run via `pnpm test` (Vitest + jsdom), typecheck via `pnpm typecheck`, lint via `pnpm lint`, build via `pnpm build` — all already in place.
