# Tasks: ng-pluralize (Pluralization Directive)

- **Specification:** `context/spec/029-ng-pluralize/`
- **Status:** Draft

---

- [x] **Slice 1: `$locale.pluralCat` foundation (filter module)**
  - [x] Widen `LocaleService` in `src/filter/locale-types.ts:137-141` with the required member `readonly pluralCat: (num: number) => string`, per technical-considerations §2.1. TSDoc documents: the number → plural-category contract, that category names are opaque lookup keys (CLDR conventionally uses `zero`/`one`/`two`/`few`/`many`/`other`), and the en-US reference implementation as a copyable one-liner for custom-locale authors. **[Agent: typescript-framework]**
  - [x] Add the en-US implementation to `defaultLocale` in `src/filter/locale.ts:139-143`: `num === 1 → 'one'`, everything else (decimals, negatives, ±Infinity, 0) → `'other'`. Stays inside the recursively frozen literal. Update the two custom-locale literals in `src/filter/__tests__/locale.test.ts:154` and `:236` with a `pluralCat` member so `pnpm typecheck` stays green. **[Agent: typescript-framework]**
  - [x] Extend the locale test suite with `pluralCat` unit coverage: `1 → 'one'`; `0`, `2`, `1.5`, `-1`, `Infinity` → `'other'`; the function survives the deep freeze (callable on the frozen literal); a swapped custom locale's `pluralCat` is reachable via `injector.get('$locale')` (precedent: `src/filter/__tests__/locale.test.ts:221-224`). **[Agent: vitest-testing]**
  - [x] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs (002–028) pass unchanged — existing `LocaleService` consumers (`currency`, `number`, `date`) read only `NUMBER_FORMATS` / `DATETIME_FORMATS` and are unaffected. **[Agent: rollup-build]**

- [x] **Slice 2: Core `ng-pluralize` directive — `when` map, exact/category selection, `{}` placeholder, switching watch (minimal end-to-end)**
  - [x] Create `src/compiler/ng-pluralize.ts` per technical-considerations §2.2–§2.4 (offset and `when-…` attributes deferred to Slices 3–4; treat offset as `0` in this slice). Exports: `NG_PLURALIZE_NAME = 'ngPluralize'` + `ngPluralizeDirective: DirectiveFactory = ['$interpolate', '$locale', '$exceptionHandler', ngPluralizeFactory]`. DDO: `restrict: 'EA'`, default priority, link-only. Link fn: read raw `attrs.count` / `attrs.when`; `whens = scope.$eval(attrs.when)` once (non-object → `{}`); per message, rewrite `{}` via `message.replace(/{}/g, startSym + '(' + countExpr + ')-0' + endSym)` using `$interpolate.startSymbol()` / `endSymbol()` and compile each with `$interpolate(...)` exactly once. Primary `scope.$watch` on the count expression: `count = parseFloat(String(newVal))`; NaN → clear text + deregister active message watch, no report, NaN→NaN no-op; else resolve key (exact `String(count)` match wins, else `$locale.pluralCat(count)`); key unchanged → no-op; key changed → deregister previous message watch, install `scope.$watch(messageFn, write)` where `write` sets `element.textContent = value ?? ''` (the `ng-bind-template` shape, `src/compiler/ng-bind-template.ts:78-103`); key with no message → clear text + route `NgPluralizeNoRuleDefinedError` via `invokeExceptionHandler($exceptionHandler, err, '$compile')` (ng-repeat in-listener precedent, `src/compiler/ng-repeat.ts:462-469`), one report per key transition. Full file-level TSDoc: algorithm, static-`when`-map contract (link-time `$eval`, runtime mutations invisible), the no-`$log` divergence, slice deferral notes. **[Agent: typescript-framework]** _(Implementation findings: (a) the compiled message table is a `Map<string, InterpolateFn>` (not a plain record) so a count resolving to a prototype key like `"constructor"` MISSES instead of finding `Object.prototype.constructor`; exact-match checks go through `messageFns.has(String(count))`. (b) `lastKey: string | null` doubles as the NaN sentinel — `null` covers both the initial state and the unusable-count state, so NaN→NaN is a no-op via the `lastKey === null` guard (upstream's `lastCount` equivalent). (c) Missing/empty `count` OR `when` returns early WITHOUT touching `textContent` — no watches, no DOM mutation, no error (upstream-lenient; tests should use elements with no initial text so "blank" holds trivially). (d) Non-string values inside the `when` map are skipped at table-build time (only `typeof message === 'string'` entries compile); a non-object `$eval` result degrades to an empty table so every valid count takes the missing-rule path. (e) The missing-rule report fires once per key TRANSITION; a NaN interlude resets `lastKey` to `null`, so uncovered-key → NaN → same-uncovered-key re-reports — documented, acceptable (development-time signal). End-to-end smoke verified: exact-key win, embedded `{{expr}}` live update without count change, numeric text `"3"` → category `other` with `{}` = 3, NaN clears.)_
  - [x] Add `NgPluralizeNoRuleDefinedError` to `src/compiler/compile-error.ts` following the `NgRepeat*Error` conventions (`readonly name` const, message carries the resolved key + the `when` source text). Re-export from `src/compiler/index.ts` and the root barrel. **[Agent: typescript-framework]** _(Constructor is `(resolvedKey: string, whenSource: string)`; message format: `ngPluralize: no rule defined for "<key>" in "<whenSource>". Add a message for that exact value or plural category.` Inserted alphabetically before the `NgRepeat*` entries in both barrels.)_
  - [x] Register the directive in `src/core/ng-module.ts`: import at top, rationale comment block + `$compileProvider.directive(NG_PLURALIZE_NAME, ngPluralizeDirective);` inside the existing `.config(['$compileProvider', …])` block — the spec-028 registration pattern (`ng-module.ts:435`). **[Agent: typescript-framework]** _(Registered immediately after the `ngRepeat` line, import slotted alphabetically between `@compiler/ng-non-bindable` and `@compiler/ng-repeat`. Verified reachable via `injector.has('ngPluralizeDirective')` in the end-to-end smoke.)_
  - [x] Create `src/compiler/__tests__/ng-pluralize.test.ts` (slice-2 scope; extended in later slices) using the `ng-bind-template.test.ts` bootstrap pattern. Coverage: registration smoke (`injector.has('ngPluralizeDirective')`); the canonical 0/1/3 message-map walk (`'0'`/`'one'`/`'other'`, FS §2.1); exact `"1"` beats `"one"` (FS §2.1); `{}` replaced with the count, repeated `{}` all replaced (FS §2.2); embedded `{{person1}}` renders and live-updates without a count change (FS §2.3); element form `<ng-pluralize>` vs attribute form `<span ng-pluralize>` produce identical text (FS §2.7); live variant switch on count 1 → 2 and placeholder refresh on 2 → 5 with the old message watch deregistered — no double-writes (FS §2.6); missing/`"abc"` count → blank with NO handler call, valid → unusable clears existing text, numeric text `"3"` behaves as `3` (FS §2.8); count 5 with only a `'one'` message → blank + handler spy receives `NgPluralizeNoRuleDefinedError` with cause `'$compile'`, page keeps digesting, one report per key transition (FS §2.9); en-US category dispatch: 1 → `one`; 0/2/1.5/−1 → `other` (FS §2.5). **[Agent: vitest-testing]** _(30 tests, all green. Coverage on `ng-pluralize.ts`: 100% lines / 95.83% branch — the sole uncovered branch is the defensive `value ?? ''` nullish fallback in the message-watch listener, unreachable through the public API because `$interpolate` always returns a string. "No double-writes" is pinned directly via an instance-level `textContent` accessor spy (borrowed `Node.prototype` descriptor, re-invoked with explicit `.call(this)`). Two extra tests pin implementation-note (d): a non-object `when` degrades to an empty table (every valid count takes the missing-rule path) and non-string map values are skipped at table-build time. The NaN-interlude re-report (uncovered → NaN → same-uncovered fires twice) is pinned as documented behavior with an inline note. No surprises — observed behavior matched the implementation notes exactly, including the exact error-message format.)_
  - [x] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. `EXCEPTION_HANDLER_CAUSES.length === 10` holds (the new error class routes via `'$compile'`). **[Agent: rollup-build]**

- [x] **Slice 3: `offset` support + `NgPluralizeBadOffsetError`**
  - [x] Extend `src/compiler/ng-pluralize.ts` per technical-considerations §2.3 step 2 / §2.4: `offset = attrs.offset` present ? `parseFloat(attrs.offset)` : `0`; a present-but-non-numeric offset routes `NgPluralizeBadOffsetError` via `$exceptionHandler('$compile')` at link time and leaves the directive inert (blank, no watches). The `{}` rewrite becomes `startSym + '(' + countExpr + ')-' + offset + endSym` (parenthesized — the documented micro-deviation from upstream's bare concatenation); category selection becomes `$locale.pluralCat(count - offset)`; exact-number keys keep matching the RAW count. Update TSDoc with the exact-raw vs. category-offset asymmetry. **[Agent: typescript-framework]** _(Implementation findings: (a) The rewrite parenthesizes BOTH operands — `startSym + '(' + countExpr + ')-(' + offset + ')' + endSym` — because a bare emit of a negative offset like `-1` would produce the unparseable `(count)--1`; `(count)-(-1)` evaluates correctly and `(count)-(0)` is semantically identical to Slice 2's `(count)-0`, so all 30 Slice 2 tests pass unchanged. (b) An empty `offset=""` counts as ABSENT (offset 0, no error) — mirrors upstream's `attr.offset || 0` and the Slice 2 emptiness rule for `count`/`when`; "present" means `typeof attrs.offset === 'string' && attrs.offset !== ''`. (c) The offset parse runs AFTER the silent missing-`count`/`when` bail, so a bad offset on an already-inert directive does not report — the LOUD path fires only when the directive would otherwise have been live. (d) Offset is link-time static (literal attribute, parsed once) — only the count is live; documented in TSDoc alongside the exact-raw vs. category-offset asymmetry and a new offset `@example` (the people-viewing quintet). Smoke-verified end-to-end: offset quintet (0→exact '0', 1→exact '1' raw, 3→pluralCat(1)='one', 4→'other' with {}=2), negative offset `-1` renders count+1, ternary count `flag ? 5 : 9` survives the parenthesized rewrite, `offset="abc"` routes the error once with cause `'$compile'` and stays blank through subsequent digests.)_
  - [x] Add `NgPluralizeBadOffsetError` to `src/compiler/compile-error.ts` (message carries the offending offset text). Re-export from `src/compiler/index.ts` and the root barrel. **[Agent: typescript-framework]** _(Constructor is `(offsetSource: string)`; message format: `ngPluralize: offset attribute value "<offsetSource>" is not a number. Provide a numeric offset or remove the attribute.` Class follows the `NgRepeat*Error` conventions (`readonly name = 'NgPluralizeBadOffsetError' as const`), placed immediately before `NgPluralizeNoRuleDefinedError` in `compile-error.ts`; barrel entries inserted alphabetically before `NgPluralizeNoRuleDefinedError` in both `src/compiler/index.ts` and `src/index.ts`. No new `EXCEPTION_HANDLER_CAUSES` token — routes via `'$compile'`, tuple stays at 10.)_
  - [x] Extend `src/compiler/__tests__/ng-pluralize.test.ts` with the offset quintet (FS §2.4): offset 2 + the canonical "people viewing" table — count 0 → exact `'0'`; count 1 → exact `'1'` with `{{person1}}` resolved; count 2 → exact `'2'`; count 3 → category `one` (3−2=1); count 4 → category `other` with `{}` showing 2. Plus: a parenthesization-sensitive count expression (e.g. ternary) renders the correct adjusted number; `offset="abc"` routes `NgPluralizeBadOffsetError` with cause `'$compile'` and the element stays blank through subsequent digests. **[Agent: vitest-testing]** _(Test findings: 11 new tests (suite 30 → 41, all green) — the `it.each` quintet plus six pins: ternary count both arms offset-adjusted, negative offset `-1` (count 2 → `{}` shows 3 via `(n)-(-1)`), `offset=""` absent (no report), `offset="2px"` → 2 (parseFloat leniency), `offset="abc"` LOUD path (exact message, `'$compile'` cause, routed ONCE at LINK time before any digest, blank/silent forever), and the inert-bail-first ordering (bad offset + no `when` → silent). Coverage on `ng-pluralize.ts`: 100% stmts/lines/funcs, 96.66% branches — sole uncovered branch is the `value ?? ''` nullish arm in the message-watch write (interpolation always yields a string; pre-existing from Slice 2, not offset-related). No surprises; implementation matched every documented behavior.)_
  - [x] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. **[Agent: rollup-build]** _(Gate findings: all four green on Node v22.22.3 / pnpm 10.6.2 — lint clean, typecheck clean, vitest 163 files / 3479 passed + 26 skipped (ng-pluralize suite at 41), Rollup build clean. Built dist verified: `NgPluralizeBadOffsetError` is a constructor on BOTH `dist/esm/index.mjs` and `dist/cjs/index.cjs` root entries (and present in `dist/types/index.d.ts` + `dist/types/compiler/index.d.ts`); `EXCEPTION_HANDLER_CAUSES.length === 10` in both module formats — no new cause token.)_

- [x] **Slice 4: Per-key `when-…` attributes (including `when-minus-N`)**
  - [x] Extend the link-time message-table build in `src/compiler/ng-pluralize.ts` per technical-considerations §2.3 step 4: scan the enumerable keys of `attrs` (enumeration yields only normalized attribute names — `$attr`/`$set`/internals are non-enumerable per `src/compiler/attributes.ts:127-190`) with `/^when(Minus)?(.+)$/`; key = `(minus ? '-' : '') + lowercase(rest)`; per-attribute entries OVERRIDE same-key `when`-map entries. First task: verify against `src/compiler/directive-normalize.ts` that digit-leading segments normalize as assumed (`when-1` → `when1`, `when-minus-1` → `whenMinus1`) and pin it with a dedicated normalization unit test. Update TSDoc with the scan rule and precedence. **[Agent: typescript-framework]** _(Findings: normalization assumption VERIFIED — `SPECIAL_CHARS_REGEX` uppercases the char after each separator and `'1'.toUpperCase() === '1'`, so `when-1` → `when1`, `when-minus-1` → `whenMinus1`; pinned with a 5-test describe appended to `src/compiler/__tests__/directive-normalize.test.ts` (including bare `when` untouched and multi-digit `when-42`) — the ng-pluralize suite was NOT touched, all 41 Slice 2–3 tests pass byte-identical. Scan shape: module-level `WHEN_ATTRIBUTE_REGEX = /^when(Minus)?(.+)$/` over `Object.keys(attrs)`, raw attribute text as message (never `$eval`ed), folded into `messageFns` AFTER the map entries so attributes override. LIVENESS RULE chosen (documented in TSDoc): inert iff `count` missing/empty OR (no non-empty `when` attr AND zero `when-…` attrs); a present-but-empty/non-object `when` keeps the directive LIVE on the FS §2.9 missing-rule path. The bail still runs BEFORE the offset parse, preserving the Slice-3 "bad offset + no when → silent" pin. Pure-attribute form passes the stand-in descriptor `'when-… attributes'` as the `NgPluralizeNoRuleDefinedError` whenSource (no `when` attr to quote). Upstream-identical edge: bare `whenMinus` attr backtracks to key `minus`. File at 422 lines; typecheck/lint/format/test all green — 3484 passed.)_
  - [x] Extend `src/compiler/__tests__/ng-pluralize.test.ts` with authoring-form coverage (FS §2.7): a directive written purely with `when-0` / `when-one` / `when-other` attributes (no `when` map) behaves identically to the equivalent map; `when` declaring `'one': "A"` + `when-one="B"` on the element → count 1 displays "B" (attribute wins); `when-minus-1` matches count −1; `when-…` attributes containing `{}` and `{{expr}}` interpolate correctly; combined map + attributes + offset works. **[Agent: vitest-testing]** _(Test findings: 12 new tests (suite 41 → 53, all green; full run 3496 passed). The five mandated cases plus seven pins: raw attribute text never `$eval`'d (`when-one="someExpr + 1"` renders literally), `when-one=""` is a valid BLANK message (live write clears initial text, NO report), a lone `when-…` attribute is a message SOURCE (live → uncovered key reports with the literal `'when-… attributes'` descriptor and exact message format), `when="{}"` keeps the directive LIVE (missing-rule report quotes `"{}"` as the source), `data-when-one` / `x-when-3` prefixed forms normalize into the scan, exact `when-minus-1` key `'-1'` beats the `other` category at count −1, and the bare `when-minus` → key `minus` backtracking quirk (unreachable entry, but counts toward liveness). Combined test pins all three precedence layers at once: map exact `'0'` raw, attribute exact `'2'` raw, attribute `'one'` OVERRIDING a map decoy at pluralCat(3−2), and `{}` = count − offset. Coverage on `ng-pluralize.ts`: 100% stmts/lines/funcs, 95.65% branches — the two uncovered branches are defensive arms unreachable via the public API: the `match[2] ?? ''` noUncheckedIndexedAccess guard (regex `.+` guarantees the group) and the pre-existing `value ?? ''` nullish fallback. No surprises — behavior matched the Slice 4 implementation notes exactly.)_
  - [x] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. **[Agent: rollup-build]** _(Gate findings: all four gates green in foreground on Node v22.22.3 — lint clean, typecheck clean, vitest 163 files / 3496 passed + 26 skipped (matches the expected Slice 4 count exactly; ng-pluralize suite at 53), Rollup build exit 0 with `.mjs` / `.cjs` / `.d.ts` emitted for every subpath entry including `compiler`.)_

- [ ] **Slice 5: Locale-swap dispatch, custom interpolation symbols, parity tests, documentation, final regression**
  - [ ] Create `src/compiler/__tests__/spec029-parity.test.ts` — the focused AngularJS-canonical regression file matching the spec 022–028 precedent. Coverage: the two canonical upstream docs examples end-to-end (message count + people-viewing-with-offset); locale swap via `createModule('app', ['ng']).factory('$locale', [() => customLocale])` with a fake multi-category `pluralCat` (e.g. mapping 1 AND 2 to a custom category) — same template, new rules (FS §2.5); custom interpolation symbols via `$interpolateProvider` — `{}` rewrite still resolves; composition smoke: `ng-pluralize` inside an `ng-repeat` row and on an `ng-if` subtree mounts/tears down cleanly (leaf directive — no structural conflict); a literal `expect(EXCEPTION_HANDLER_CAUSES.length).toBe(10)` regression guard. **[Agent: vitest-testing]**
  - [ ] Update `src/compiler/README.md` — new "Pluralization directive (spec 029)" section: selection algorithm (exact-raw vs. category-offset), the `{}` rewrite (parenthesized deviation), the `when-…` attribute scan, the switching-watch design, the `$locale.pluralCat` seam + custom-locale recipe, the no-`$log` divergence (handler-routed `NgPluralizeNoRuleDefinedError`), worked examples. **[Agent: typedoc-docs]**
  - [ ] Update `CLAUDE.md`: amend the `./compiler` Modules row (directive count 45 → 46, add the two error classes to the export list), add "Non-obvious invariants" bullets — (a) `pluralCat` is a REQUIRED `LocaleService` member (the spec-029 `.d.ts` break, with rationale), (b) exact keys match the raw count while categories and `{}` use count − offset, (c) missing-rule reports route via `'$compile'` once per key transition (no `$log`), (d) the `when` map is link-time static — and add "Where to look when…" rows for the directive and `pluralCat`. Tick the `ng-pluralize` roadmap item in `context/product/roadmap.md`. **[Agent: typedoc-docs]**
  - [ ] TSDoc audit on every new public surface — `LocaleService.pluralCat`, the two error classes, the directive's file-level TSDoc. Each carries at least one runnable `@example`. **[Agent: typedoc-docs]**
  - [ ] Final regression: `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All five gates pass; full prior-spec suite (002–028) unchanged; `EXCEPTION_HANDLER_CAUSES.length === 10` held. New observable: `<ng-pluralize>` / `[ng-pluralize]` in all forms (map, attributes, offset, locale-swapped) works end-to-end. **[Agent: rollup-build]**

---

## Notes for the Implementation Agent

- **No new `EXCEPTION_HANDLER_CAUSES` entry.** Both new error classes (`NgPluralizeNoRuleDefinedError`, `NgPluralizeBadOffsetError`) route via the existing `'$compile'` cause token. The tuple stays at 10.
- **`pluralCat` is a required-field `.d.ts` break** — accepted deliberately (pre-1.0, upstream parity; an optional field would silently fall back to English rules). The two custom-locale test literals are the only in-repo call sites needing updates.
- **No `$log` service exists in this project.** Upstream's `$log.debug` for missing rules is replaced by handler-routed `NgPluralizeNoRuleDefinedError` — a documented divergence, reported once per key *transition*, never on NaN counts.
- **The `(countExpr) - offset` rewrite is parenthesized** — a documented micro-deviation from upstream's bare concatenation; identical semantics for all upstream-legal inputs.
- **The `when` map is static** (one link-time `$eval`, upstream parity); the count and embedded `{{expr}}` bindings are live.
- **`ng-pluralize` is a leaf text-writer** — not `transclude: 'element'`, not terminal; it composes with structural hosts like `ng-class` does and does not widen the spec-027 same-element gap.
- **Tick task checkboxes in the SAME commit as the implementation** per `CLAUDE.md` — the co-modification of `tasks.md` + `src/` in one commit is the traceability signal the audit looks for.
