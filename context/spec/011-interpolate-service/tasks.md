# Tasks: `$interpolate` Service — String & Template Interpolation

- **Specification:** `context/spec/011-interpolate-service/`
- **Status:** Draft

---

- [x] **Slice 1: Minimal ES Module `interpolate` — Default Delimiters, Basic Rendering**
  - [x] Create `src/core/utils.ts` helper `toInterpolationString(value: unknown): string` — `undefined`/`null` → `''`, strings as-is, numbers/booleans/functions via `String(value)`, objects/arrays via `JSON.stringify`. Export from `src/core/index.ts`. **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/interpolate-scanner.ts` with `scan(text, startSymbol, endSymbol): { textSegments: string[]; expressions: string[] }`. Support plain text, single expression, multiple expressions, adjacent expressions. No escape handling yet. Throw `"Unterminated expression in interpolation: <source>"` for an opening marker without a matching close. Throw `"Empty expression in interpolation string: <source>"` for whitespace-only expression bodies. **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/interpolate-types.ts` with `InterpolateOptions` (`{ startSymbol?: string; endSymbol?: string }`), `InterpolateFn` (callable `(context) => string | undefined` with `.exp: string`, `.expressions: string[]`, `.oneTime: boolean` — fields populated in later slices; for now `exp` and `expressions` are set, `oneTime` is always `false`), and `InterpolateService` (callable with signature `(text, mustHaveExpression?, trustedContext?, allOrNothing?) => InterpolateFn | undefined`, with `.startSymbol()` / `.endSymbol()` getters). **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/interpolate.ts` exporting `createInterpolate(options?: InterpolateOptions): InterpolateService` with default delimiters `{{` / `}}`. Scan → `parse()` each expression → build a render fn that stringifies results via `toInterpolationString` and concatenates with text segments. Attach `.startSymbol()` / `.endSymbol()` getters to the service. This slice supports ONLY the basic case (no `mustHaveExpression`, no `allOrNothing`, no escape, no oneTime). **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/index.ts` re-exporting `createInterpolate`, `interpolate` (pre-configured convenience — `const interpolate = createInterpolate()`), and the three public types. **[Agent: typescript-framework]**
  - [x] Extend `src/index.ts` to re-export `createInterpolate`, `interpolate`, and the types. **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/__tests__/interpolate-scanner.test.ts` with tests for plain text, single expression, multiple expressions, adjacent expressions (`{{a}}{{b}}`), unterminated marker throws, empty marker throws, `::` prefix retained verbatim. **[Agent: vitest-testing]**
  - [x] Create `src/interpolate/__tests__/interpolate-esm.test.ts` with basic tests: `createInterpolate()('Hello')({})` → `'Hello'`; `createInterpolate()('Hello {{name}}')({name: 'Alice'})` → `'Hello Alice'`; `interpolate('{{a}} and {{b}}')({a: 1, b: 2})` → `'1 and 2'`; stringification rules (undefined/null → `''`, arrays → JSON, etc.); statelessness across calls. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 2: Custom Delimiters + Escape Sequences**
  - [x] Extend `interpolate-scanner.ts` to handle backslash-escaped delimiter characters: `\{\{` and `\}\}` produce literal `{{` / `}}` in the text segments and do NOT open/close expressions. Match AngularJS `angular.js:src/ng/interpolate.js` escape semantics. **[Agent: typescript-framework]**
  - [x] Extend `createInterpolate` in `interpolate.ts` to validate `options.startSymbol` and `options.endSymbol` at call time: reject empty strings and identical symbols with descriptive errors. **[Agent: typescript-framework]**
  - [x] Extend `interpolate-scanner.test.ts` with: custom delimiters `[[` / `]]`, escape sequences inside literal text, escape sequences inside expression body (no effect), mixed escape + expression (`{{a}} and \{\{literal\}\}`). **[Agent: vitest-testing]**
  - [x] Extend `interpolate-esm.test.ts` with: `createInterpolate({ startSymbol: '[[', endSymbol: ']]' })('Hi [[name]]')({name: 'Bob'})` → `'Hi Bob'`; validation errors for empty and identical delimiters; escape-sequence rendering `'\\{\\{literal\\}\\}'` → `'{{literal}}'`. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 3: `mustHaveExpression` and `allOrNothing` Flags**
  - [x] Extend the service call in `interpolate.ts`: if `mustHaveExpression === true` and `expressions.length === 0`, return `undefined` instead of a function. TypeScript overload so that `mustHaveExpression: true` narrows the return to `InterpolateFn | undefined` and `false`/omitted narrows to `InterpolateFn`. **[Agent: typescript-framework]**
  - [x] Extend the render function: if `allOrNothing === true` and ANY expression result is strictly `undefined`, return `undefined`. `null` does NOT trigger this (AngularJS parity). **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/__tests__/interpolate.test.ts` with tests: `mustHaveExpression === true` on plain text → `undefined`; on text with markers → callable fn; whitespace-only plain text with flag → `undefined`. `allOrNothing === true` with single undefined expr → `undefined`; with all defined → rendered string; `null` does NOT trigger (renders as `''`); `allOrNothing` on no-expression text still renders. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 4: Metadata Properties + `oneTime` Render Semantics**
  - [x] Compute `oneTime` in `interpolate.ts`: `expressions.length > 0 && parsedFns.every(fn => fn.oneTime)`. Attach as `.oneTime` on the returned `InterpolateFn`. Also populate `.exp` (verbatim input) and `.expressions` (raw source strings with `::` retained). **[Agent: typescript-framework]**
  - [x] Extend the render function: when `oneTime === true` AND any parsed expression evaluates to `undefined`, return `undefined` (not `''`). Once ALL are defined, return the concatenated string. `allOrNothing` remains orthogonal and layers on top. Document this in JSDoc on `InterpolateFn`. **[Agent: typescript-framework]**
  - [x] Extend `interpolate.test.ts` with metadata tests: `.exp` returns verbatim text; `.expressions` returns raw sources (`['name']`, `['::name']`, `['name', 'age']`, `[]`); `.oneTime` is `true` only when ALL embedded expressions are `::`-prefixed; no expressions → `.oneTime === false`; mixed `::` and non-`::` → `.oneTime === false`. **[Agent: vitest-testing]**
  - [x] Add render-semantics tests: `createInterpolate()('Hello {{::name}}')({})` → `undefined` (not stabilized); same called with `{name: 'Alice'}` → `'Hello Alice'`; all-`::` with two expressions — only returns string when both defined; non-oneTime template renders normally regardless (undefined → `''`). **[Agent: vitest-testing]**
  - [x] Add parse-error propagation test: `createInterpolate()('Hello {{a +}}')` throws synchronously from `parse()`. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 5: `$InterpolateProvider` + `ng` Core Module + DI Integration**
  - [x] Create `src/interpolate/interpolate-provider.ts` exporting the `$InterpolateProvider` class. Fields: `private options: InterpolateOptions`. Methods: `startSymbol(value?: string)` and `endSymbol(value?: string)` — fluent on set (returns `this`), getter when called with no args; validate values identically to `createInterpolate` (non-empty, not identical). `$get` property (array-style with no DI deps, or a plain function) that returns `createInterpolate(this.options)`. **[Agent: typescript-framework]**
  - [x] Create `src/core/ng-module.ts` creating `ngModule = createModule('ng', [])` and registering `$interpolate` via `.provider('$interpolate', $InterpolateProvider)`. Export `ngModule` from `src/core/index.ts` and from `src/index.ts`. **[Agent: typescript-framework]**
  - [x] Accept `trustedContext` as a no-op placeholder on all signatures (already in Slice 1 types; add a `// TODO(spec-$sce)` comment at the single no-op site in `interpolate.ts` for discoverability). **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/__tests__/interpolate-provider.test.ts` with provider unit tests: default `startSymbol()` / `endSymbol()`; fluent setters; validation errors for empty and identical symbols; `$get` returns a service that uses the configured symbols. **[Agent: vitest-testing]**
  - [x] Create `src/interpolate/__tests__/interpolate-di.test.ts` with DI integration tests: `createInjector([ngModule])` exposes `$interpolate` at run time; `$interpolateProvider` accessible only during `config()` phase; symbols set via `config(['$interpolateProvider', fn])` are observed by the service; DI-path output matches ES-module-path output (parity guard using the same template + context). **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 6: Scope `$watch` Integration for Function-Form oneTime Watchers**
  - [x] Verify in `src/core/scope.ts` that `compileToWatchFn` already passes function inputs through unchanged so an `InterpolateFn` with `.oneTime === true` is routed through the existing spec-010 flag-inspection branch in `$watch` and dispatched to `oneTimeWatchDelegate`. If it does NOT (because the branch only inspects flags on strings), extend the function-input branch to preserve/pass through `.oneTime` so the routing works. Add a JSDoc note that function-form flagged watchers are a supported pattern. **[Agent: typescript-framework]**
  - [x] Create `src/interpolate/__tests__/interpolate-watch.test.ts` with `$watch` integration tests: (a) `$watch($interpolate('Hello {{name}}'), listener)` — non-oneTime, fires on rendered-string changes; (b) `$watch($interpolate('Hello {{::name}}'), listener)` — fires once `scope.name` is defined, then deregisters post-digest; subsequent changes do NOT fire; (c) all-`::` with two expressions — deregisters only after BOTH are defined; (d) mixed `::` and non-`::` behaves as non-oneTime (watcher stays live); (e) manual deregister before stabilization — listener never fires; (f) `$watch` against DI-resolved `$interpolate` (from Slice 5 `ngModule`) behaves identically. **[Agent: vitest-testing]**
  - [x] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [x] **Slice 7: AngularJS Parity Cross-Reference + Final Build Verification**
  - [x] Cross-reference `angular/angular.js/test/ng/interpolateSpec.js`. Port any scenarios not already covered by slices 1–6 (escape edge cases, multiple custom-delimiter cases, `allOrNothing` + oneTime interactions, `toJson` parity for objects/arrays). Add to the appropriate existing test file. **[Agent: vitest-testing]**
  - [x] Cross-reference `angular/angular.js/test/ng/rootScopeSpec.js` for any `$interpolate` + `$watch` integration scenarios not covered by Slice 6. Port into `interpolate-watch.test.ts`. **[Agent: vitest-testing]**
  - [x] Run `pnpm build` and verify `dist/esm/*`, `dist/cjs/*`, `dist/types/*` generate successfully. Confirm `dist/types/interpolate/index.d.ts` exposes `createInterpolate`, `interpolate`, `InterpolateFn`, `InterpolateService`, `InterpolateOptions`. Confirm `dist/types/core/ng-module.d.ts` exposes `ngModule`. **[Agent: rollup-build]**
  - [x] Final verification: `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build` all pass. Full regression pass against pre-existing tests (specs 002, 003, 006, 007, 008, 009, 010). **[Agent: typescript-framework]**
