# Tasks: `$sanitize` — HTML Sanitization Service

- **Specification:** `context/spec/013-sanitize-service/`
- **Status:** Draft

---

- [ ] **Slice 1: Path Alias + Allow-List Constants**
  - [ ] Add path alias `@sanitize/*` → `src/sanitize/*` in `tsconfig.json`, `vitest.config.ts`, and `rollup.config.mjs` (`tsPathAliases` entry only — the build entry comes in Slice 8). **[Agent: rollup-build]**
  - [ ] Create `src/sanitize/sanitize-allow-lists.ts` exporting frozen `Set<string>` constants ported from `angular/angular.js/src/ngSanitize/sanitize.js` v1.8.3: `VOID_ELEMENTS`, `OPTIONAL_END_TAG_BLOCK_ELEMENTS`, `OPTIONAL_END_TAG_INLINE_ELEMENTS`, `BLOCK_ELEMENTS`, `INLINE_ELEMENTS`, `VALID_ELEMENTS` (union), `VALID_ATTRS`, `URI_ATTRS`, `SVG_ELEMENTS`, `SVG_ATTRS`, plus the `DEFAULT_URI_PATTERN: RegExp`. Each constant frozen via `Object.freeze` on the underlying array before constructing the `Set`, and the resulting `Set` references retained as module-scope `const`s. Top-of-file TSDoc cites the upstream source path + version. **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/sanitize-types.ts` with scaffolding types: `AddValidElementsArg = string | string[] | { htmlVoidElements?: string[]; htmlElements?: string[]; svgElements?: string[] }`, plus placeholder `SanitizeService = (input: unknown) => string` and `SanitizeOptions = { extraValidElements?: readonly string[]; extraValidAttrs?: readonly string[]; svgEnabled?: boolean; uriPattern?: RegExp }`. **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/index.ts` re-exporting nothing yet beyond `SanitizeService` / `SanitizeOptions` / `AddValidElementsArg` types — concrete value exports added in subsequent slices. **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/__tests__/sanitize-allow-lists.test.ts` covering: (a) each constant exists and is a `Set<string>`; (b) each constant's contents match a hand-copied AngularJS 1.8.3 reference array (use `toEqual([...VALID_ELEMENTS].sort())`); (c) the underlying constants are frozen — mutating the source array reference does not affect the exported Set; (d) `DEFAULT_URI_PATTERN` matches representative safe URLs (`http://x.com`, `mailto:x@y.com`, `tel:+1`, `/relative`, `./relative`, `#anchor`) and rejects representative dangerous URLs (`javascript:alert(1)`, `data:text/html,<script>`, `vbscript:msgbox(1)`); (e) protocol comparison is case-insensitive (`HTTPS:` matches the regex). **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 2: Regex Tokenizer (`htmlParser`)**
  - [ ] Create `src/sanitize/sanitize-tokenizer.ts` exporting `htmlParser(html: string, handler: TokenHandler): void` and the `TokenHandler` interface with `start(tagName, attrs, unary)`, `end(tagName)`, `chars(text)`, `comment(text)` methods. Faithful port of AngularJS 1.x `htmlParser`: regex sources copied verbatim from upstream (`START_TAG_REGEXP`, `END_TAG_REGEXP`, `ATTR_REGEXP`, etc.); state machine is a `while` loop with a tag stack for auto-close; consumes `OPTIONAL_END_TAG_BLOCK_ELEMENTS` / `OPTIONAL_END_TAG_INLINE_ELEMENTS` to gate implicit closure; comments and CDATA emit their corresponding events; entities pass through verbatim in `chars`; malformed input recovers via auto-close, never throws. Variable names mirror upstream so the line-by-line port is traceable. The tokenizer is allow-list-agnostic — it does NOT decide whether a tag survives, only emits events. **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/__tests__/sanitize-tokenizer.test.ts` covering: plain text → single `chars` event; one tag → matched `start`/`end` pair; void elements (`<br>`, `<img/>`) emit `start` with `unary: true`; nested tags emit in stack order; mismatched closing tags auto-close intervening; unclosed tags at end-of-input drain the stack; comments emit `comment`; CDATA falls through as `chars`; entities (`&amp;`, `&lt;`, `&#x3c;`) survive verbatim in `chars`; attribute parsing covers single-quoted / double-quoted / unquoted / no-value variants and decodes them into the `attrs` Map; case-insensitive tag-name normalization to lowercase. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 3: `createSanitize` ES Factory + Default `sanitize` Export**
  - [ ] Create `src/sanitize/sanitize.ts` exporting `createSanitize(options?: SanitizeOptions): SanitizeService` and `sanitize: SanitizeService` (= `createSanitize()`). Factory: resolve effective tag/attr sets at call time (defaults ∪ extras ∪ optional SVG); freeze them; resolve effective URI regex (`options.uriPattern ?? DEFAULT_URI_PATTERN`). Returned service: coerce input (`null`/`undefined` → `''`, otherwise `String(input)`); empty → `''`; otherwise drive `htmlParser` with an inline `TokenHandler` that consults the effective sets — `start` writes `<tag` only if allowed (otherwise increments `dropDepth`), walks attrs and writes only allow-listed names (URI attrs additionally validated against the regex with whitespace-trimmed value); `end` decrements `dropDepth` or writes `</tag>`; `chars` entity-escapes via a hand-rolled `encodeEntities` (matching AngularJS `encodeEntities` exactly) unless `dropDepth > 0`; `comment` drops unconditionally. Service is a closure, NOT a class — safely destructurable. **[Agent: typescript-framework]**
  - [ ] Add `createSanitize` and `sanitize` exports to `src/sanitize/index.ts`. Also extend `src/index.ts` to re-export `createSanitize`, `sanitize`, and the `SanitizeService` / `SanitizeOptions` types. **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/__tests__/sanitize-esm.test.ts` covering: `sanitize('plain text') === 'plain text'`; `sanitize('') === ''`; `sanitize(null) === ''`; `sanitize(undefined) === ''`; `sanitize(42 as unknown as string) === '42'` (coerced); `sanitize('<p>hi</p>') === '<p>hi</p>'`; `sanitize('<script>alert(1)</script>x')` drops the script + contents and returns `'x'`; `sanitize('<a href="javascript:alert(1)">x</a>') === '<a>x</a>'`; `sanitize('<a href="https://example.com/">x</a>')` survives; `sanitize('<img src="x.png" onerror="alert(1)">')` drops `onerror`; `sanitize('<style>body{x:y}</style>x')` drops style + contents; `sanitize('<div onclick="x">y</div>')` drops `onclick`; `createSanitize({ svgEnabled: true })` accepts `<svg><circle/></svg>`; default `createSanitize()` strips them; `createSanitize({ extraValidElements: ['my-tag'] })` allows `<my-tag>x</my-tag>` to survive; `createSanitize({ uriPattern: /^myapp:/ })` accepts only `myapp:` URLs; idempotence — `sanitize(sanitize(x)) === sanitize(x)` for representative inputs. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 4: `$SanitizeProvider` + `ngSanitize` Module + DI Integration**
  - [ ] Create `src/sanitize/sanitize-provider.ts` exporting the `$SanitizeProvider` class. Instance state: `$$extraValidElements: Set<string>`, `$$extraValidAttrs: Set<string>`, `$$svgEnabled: boolean = false`, `$$uriPattern: RegExp = DEFAULT_URI_PATTERN`. Public methods (overloaded fluent setters, mirroring `$SceDelegateProvider`): `addValidElements(arg: AddValidElementsArg): this` (decomposes the object form into the right buckets — html/svg sets), `addValidAttrs(attrs: string[]): this`, `enableSvg(value: boolean): this`, `uriPattern(pattern: RegExp): this`. Each setter validates synchronously (typeof, instanceof, array shape) and throws `Error` with `$sanitizeProvider.<method>: …` prefix on bad input. `$get = [(): SanitizeService => createSanitize({ extraValidElements: [...this.$$extraValidElements], extraValidAttrs: [...this.$$extraValidAttrs], svgEnabled: this.$$svgEnabled, uriPattern: this.$$uriPattern })] as const`. **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/ng-sanitize-module.ts` exporting `ngSanitize = createModule('ngSanitize', []).provider('$sanitize', $SanitizeProvider)`. Add the `declare module '@di/di-types'` augmentation registering `$sanitize` / `$sanitizeProvider` under the `ngSanitize` registry shape (mirrors the spec-012 ng-module pattern). **[Agent: typescript-framework]**
  - [ ] Add `$SanitizeProvider` and `ngSanitize` exports to `src/sanitize/index.ts`. Add `ngSanitize` (value) to `src/index.ts` root re-exports. `$SanitizeProvider` stays out of the root barrel (reachable via `injector.get('$sanitizeProvider')` only). **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/__tests__/sanitize-provider.test.ts` covering: defaults (`enableSvg() === false`, `uriPattern()` returns `DEFAULT_URI_PATTERN`); fluent chaining `provider.addValidElements(['x']).addValidAttrs(['y']).enableSvg(true)`; setter validation (each of the four with appropriate bad inputs throws synchronously); idempotent calls (setting the same value twice does not duplicate Set entries); `$get` produces a `SanitizeService` that respects the configured extras (e.g. after `addValidElements(['my-tag'])`, the resulting service preserves `<my-tag>`). **[Agent: vitest-testing]**
  - [ ] Create `src/sanitize/__tests__/sanitize-di.test.ts` covering: `createInjector([ngModule, ngSanitize])` exposes `$sanitize` (callable, singleton across `injector.get` calls); `injector.has('$sanitize')` returns `true` when `ngSanitize` is loaded, `false` otherwise; `createInjector([ngModule])` (no `ngSanitize`) — `injector.get('$sanitize')` throws "Unknown provider"; `injector.get('$sanitizeProvider')` throws at run-phase ("Unknown provider"); `config(['$sanitizeProvider', p => p.enableSvg(true)])` is observed at runtime; ESM/DI parity guard — for representative inputs, DI-resolved `$sanitize` and ESM `sanitize` default export produce identical outputs. Use the `beforeEach(() => { resetRegistry(); createModule('ngSanitize', []).provider('$sanitize', $SanitizeProvider); })` pattern (mirrors `interpolate-di.test.ts`). **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 5: `$sce` ↔ `$sanitize` Fallback Integration**
  - [ ] Modify `src/sce/sce-types.ts`: extend `SceOptions` with `readonly sanitize?: (html: string) => string`. Add a TSDoc `@example` showing the pure-ESM wiring (`createSce({ sanitize })`). **[Agent: typescript-framework]**
  - [ ] Modify `src/sce/sce.ts` `createSce` factory: capture `options?.sanitize` into a closure variable. In the `getTrusted` closure, after the `!enabled` early return and the nullish pass-through, but BEFORE delegating to `delegate.getTrusted`, add the html-context branch: if `ctx === SCE_CONTEXTS.HTML && sanitize !== undefined && typeof value === 'string'`, return `sanitize(value)`. All other paths unchanged. **[Agent: typescript-framework]**
  - [ ] Modify `src/sce/sce-provider.ts` `$get`: add `'$injector'` as a dep alongside `'$sceDelegate'`. Inside the factory function, do `const sanitize = $injector.has('$sanitize') ? ($injector.get('$sanitize') as (html: string) => string) : undefined;` and pass into `createSce({ delegate, enabled: this.$$enabled, sanitize })`. Update the TSDoc on `$get` to mention the new dep and the lazy fallback. **[Agent: typescript-framework]**
  - [ ] Create `src/sanitize/__tests__/sanitize-sce.test.ts` covering: `createInjector([ngModule, ngSanitize])` — `$sce.getTrustedHtml('plain <p>x</p>')` returns sanitized string (no throw); `createInjector([ngModule])` — `$sce.getTrustedHtml('<p>x</p>')` throws (preserves spec-012 behavior); `$sce.getTrustedHtml(trustedWrapper)` always unwraps directly even when `$sanitize` is loaded (sanitize NOT invoked on wrapped values — verify via spy or mock); `$sce.getTrustedHtml(null)` and `…(undefined)` continue to pass through; strict mode OFF (`config(['$sceProvider', p => p.enabled(false)])`) — `$sce.getTrustedHtml('<script>x</script>x')` returns the input unchanged (sanitize NOT invoked); other contexts (`getTrustedUrl`, `getTrustedJs`, `getTrustedCss`, `getTrustedResourceUrl`) are unaffected by the integration; ESM-first equivalent — `createSce({ sanitize })` produces the same fallback behavior without DI involvement. **[Agent: vitest-testing]**
  - [ ] Update existing `src/sce/__tests__/sce-di.test.ts` and `src/sce/__tests__/sce-provider.test.ts` if they construct `$SceProvider.$get` with a positional-args expectation that will break given the new `$injector` dep. Add `$injector` to their `$get` invocation sites accordingly. Test fixture-only updates; no behavior assertions change. **[Agent: vitest-testing]**
  - [ ] Regression: re-run all spec-012 `$sce` tests — must pass unchanged when `ngSanitize` is NOT loaded. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 6: AngularJS Parity Test Suite**
  - [ ] Create `src/sanitize/__tests__/parity-spec.test.ts` porting test vectors from `angular/angular.js/test/ngSanitize/sanitizeSpec.js` at v1.8.3. Each upstream `it(...)` becomes a Vitest `it(...)` with the same input and expected output; input strings are hand-copied verbatim, expected output is recomputed and pinned. Group by upstream `describe` blocks for traceability. Top-of-file comment links to the upstream source and the version pinned. Any intentional deviations (e.g. AngularJS-specific edge cases that don't apply to our ESM-first design) are annotated inline with a `// DEVIATION:` comment plus rationale. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 7: CVE-Regression Test Suite**
  - [ ] Create `src/sanitize/__tests__/cve-regressions.test.ts` with one test per documented historical `ngSanitize` advisory. At minimum: **CVE-2020-7676** (mXSS via `<noscript>` namespace confusion), **CVE-2018-12116** (protocol-relative URL bypass), **CVE-2014-3506** (sandbox escape via form action), plus any additional advisories enumerated in `angular/angular.js`'s GitHub Security Advisories tab and `CHANGELOG.md` lines matching `xss` / `sanitize`. Each test: (a) embeds the original PoC payload as a verbatim string constant; (b) asserts `sanitize(payload)` returns the documented safe output; (c) carries a `// CVE-YYYY-NNNNN: <one-line description>` comment with the advisory URL. Top-of-file block comment lists every advisory link so future maintainers can re-investigate before changing tests. **[Agent: vitest-testing]**
  - [ ] `pnpm lint`, `pnpm typecheck`, `pnpm test` must pass. **[Agent: typescript-framework]**

- [ ] **Slice 8: Docs + Build Output + Final Verification**
  - [ ] Update `CLAUDE.md`: add `./sanitize` to the Modules table with a one-liner on the opt-in nature + ESM/DI entry points; add a "Non-obvious invariants" bullet stating that `ngSanitize` is opt-in and `$sce.getTrustedHtml` integrates via lazy `$injector.has` lookup in `$SceProvider.$get` — no hard dependency, no decoration; add a "Where to look when..." row pointing to `src/sanitize/sanitize.ts` and `src/sanitize/sanitize-tokenizer.ts` for "How is untrusted HTML scrubbed?". **[Agent: typedoc-docs]**
  - [ ] Add the `./sanitize` subpath entry to `package.json` `exports` map (alongside `./sce`, `./interpolate`, etc.). Add `{ name: 'sanitize/index', input: 'src/sanitize/index.ts' }` to the `entries` array in `rollup.config.mjs`. **[Agent: rollup-build]**
  - [ ] Run `pnpm build` and verify `dist/{esm,cjs,types}/sanitize/index.{mjs,cjs,d.ts}` are generated. Spot-check `dist/types/sanitize/index.d.ts` exposes `createSanitize`, `sanitize`, `$SanitizeProvider`, `ngSanitize`, `SanitizeService`, `SanitizeOptions`, `AddValidElementsArg`. Spot-check the updated `dist/types/sce/sce-types.d.ts` declares the new optional `sanitize` callback on `SceOptions`. **[Agent: rollup-build]**
  - [ ] Final verification: `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test`, `pnpm build` all pass. Full regression pass against pre-existing tests (specs 002, 003, 006, 007, 008, 009, 010, 011, 012). Confirm `src/sanitize/` line coverage ≥ 90% via `pnpm test -- --coverage` (or update the project's existing coverage script). **[Agent: typescript-framework]**
