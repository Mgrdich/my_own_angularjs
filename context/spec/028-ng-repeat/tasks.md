# Tasks: ng-repeat (List Iteration Directive)

- **Specification:** `context/spec/028-ng-repeat/`
- **Status:** Draft

---

- [ ] **Slice 1: Iterator expression parser + error classes (foundation)**
  - [ ] Create `src/compiler/ng-repeat-iterator-parse.ts` per technical-considerations §2.1. Exports module-private `parseIteratorExpression(raw: string)` returning a `ParsedIteratorExpression` record `{ keyIdent: string | null, valueIdent: string, collectionExpr: ParsedExpression, aliasIdent: string | null, trackByExpr: ParsedExpression | null }`. Top-level regex matches `^\s*([\s\S]+?)\s+in\s+([\s\S]+?)(?:\s+as\s+([\s\S]+?))?(?:\s+track\s+by\s+([\s\S]+?))?\s*$`. LHS sub-parse for the `item` vs `(key, value)` variants using the shared `IDENT_RE` from `@controller/controller.ts`. Sub-expressions for `<collection>` and `<track by>` parse via `parse()` from `@parser/index`. Full file-level TSDoc covering the grammar, the four sub-component splits, and the error-class mapping. **[Agent: typescript-framework]**
  - [ ] Add three error classes to `src/compiler/compile-error.ts`: `NgRepeatBadIteratorExpressionError`, `NgRepeatBadIdentifierError`, `NgRepeatBadAliasError`. Each carries the offending raw expression in the message. Each is a `class … extends Error` matching the existing error-class pattern in the file. TSDoc explains the trigger condition for each. **[Agent: typescript-framework]**
  - [ ] Re-export the three new error classes from `src/compiler/index.ts` and `src/index.ts` (root barrel). No directive factory in this slice — only the parser and error surface. **[Agent: typescript-framework]**
  - [ ] Create `src/compiler/__tests__/ng-repeat-iterator-parse.test.ts`. Coverage: basic `item in list`; `(key, value) in obj`; `item in list as alias`; `item in list track by item.id`; `item in list as alias track by item.id`; `(key, value) in obj track by k`; filter chains in the collection (`todo in todos | filter:q`); whitespace tolerance; `NgRepeatBadIteratorExpressionError` thrown for missing `in`, wrong clause order (`track by … as …`), unparenthesized `key, value`; `NgRepeatBadIdentifierError` thrown for empty item name, punctuation-containing name, JS-reserved word; `NgRepeatBadAliasError` thrown for alias colliding with the item / key / value name in the same expression. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs (002–027) pass unchanged. `EXCEPTION_HANDLER_CAUSES.length === 10` holds. **[Agent: rollup-build]**

- [ ] **Slice 2: Default identity tracker**
  - [ ] Create `src/compiler/ng-repeat-identity.ts` per technical-considerations §2.2. Exports `createIdentityTracker()` returning `{ getIdentity(value: unknown): string }`. Implementation: closure-local `WeakMap<object, string>` for object items; monotonic counter producing identities like `'object:1'`, `'object:2'`. Primitive items mapped to type-prefix sentinels: `'string:<value>'`, `'number:<value>'`, `'boolean:<value>'`, `'null:'`, `'undefined:'`, `'bigint:<value>'`, `'symbol:<description>'`. `NaN` normalized to `'number:NaN'`; `+0`/`-0` normalized to `'number:0'`. Full file-level TSDoc covering the WeakMap-vs-`$$hashKey` divergence (deliberate; user data is not mutated). **[Agent: typescript-framework]**
  - [ ] Create `src/compiler/__tests__/ng-repeat-identity.test.ts`. Coverage: same object returns same identity across calls; distinct objects return distinct identities; mutating an object (changing its properties) does NOT change its identity (reference-based); primitives map to type-prefix sentinels; `NaN` normalized; `+0` / `-0` collapse to the same sentinel; `null` vs `undefined` are distinct; class instances and arrays are treated as objects (WeakMap branch); a fresh tracker has no shared state with a previous tracker. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. **[Agent: rollup-build]**

- [ ] **Slice 3: `ng-repeat` directive — basic `item in list` + locals + duplicate detection (minimal end-to-end)**
  - [ ] Create `src/compiler/ng-repeat.ts` per technical-considerations §2.3 / §2.4. Module-private `NG_REPEAT_NAME = 'ngRepeat'` constant. Factory shape `[() => ({ … })]` (zero-dep array-form). Directive metadata: `restrict: 'A'`, `priority: 1000`, `terminal: true`, `transclude: 'element'`. Link fn: verify `isComment(element)` (throw on mismatch matching the recent hardening precedent); defensive bail if `$transclude === undefined`; parse the iterator expression once via `parseIteratorExpression`; create a fresh `createIdentityTracker()`. Closure-local `currentRows: Map<string, RowEntry>` where `RowEntry = { scope, cloneRoot, index, value, key? }`. `scope.$watchCollection(parsed.collectionExpr, (newCollection) => reconcile(newCollection))`. `reconcile` for THIS slice: arrays only (object branch deferred to Slice 5); no `track by` consumption (identity tracker only — Slice 4 wires `track by`); no alias publication (deferred to Slice 6); duplicate-key detection throws `NgRepeatDuplicateKeyError`; tear-down on non-iterable values; per-row scope = `parentScope.$new()` carrying `$index`, `$first`, `$last`, `$middle`, `$even`, `$odd`, and `scope[parsed.valueIdent] = item`. Wrap the reconciliation in `try { reconcile(...) } catch (err) { invokeExceptionHandler($eh, err, '$compile') }` so the duplicate-key throw routes through `'$compile'`, NOT `'watchListener'`. Register `addElementCleanup(placeholder, tearDownAllRows)` and `scope.$on('$destroy', tearDownAllRows)`. Full file-level TSDoc covering: the foundation, the reconciliation algorithm, the deferral notes for slices 4–6, the duplicate-detection contract, the cleanup contract. **[Agent: typescript-framework]**
  - [ ] Add `NgRepeatDuplicateKeyError` to `src/compiler/compile-error.ts` (the 4th new error class in this spec). Message includes the duplicate identity, the two offending items, and a `track by` suggestion. Re-export from `@compiler/index` and root barrel. **[Agent: typescript-framework]**
  - [ ] Register `ngRepeatDirective` in `src/core/ng-module.ts` via `$compileProvider.directive(NG_REPEAT_NAME, ngRepeatDirective)`. Alphabetized into the existing structural-directive block. **[Agent: typescript-framework]**
  - [ ] Create `src/compiler/__tests__/ng-repeat.test.ts` (slice-3 scope only — extended in later slices). Coverage: registration sanity (`injector.has('ngRepeatDirective') === true`); basic `item in list` produces N rows in order; rendered text content matches; the six per-row locals work (`$index + 1`, `$first`/`$last` boundary classes, `$even`/`$odd` zebra-striping); appending an item appends a row; removing an item removes the matching row; replacing the collection with a non-iterable value (`null`, `undefined`, a number) clears all rows with no error; replacing a list of unique primitives works; `[1, 2, 2, 3]` (duplicate primitives, no `track by`) throws `NgRepeatDuplicateKeyError` routed via `$exceptionHandler('$compile')`; restrict `'A'` enforced (no element-form match); `terminal: true` blocks lower-priority same-element directives. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. `EXCEPTION_HANDLER_CAUSES.length === 10` holds (the four new error classes all route via `'$compile'`). **[Agent: rollup-build]**

- [ ] **Slice 4: Custom identity via `track by EXPR` + row reuse**
  - [ ] Extend the reconciliation engine in `src/compiler/ng-repeat.ts` to evaluate the parsed `trackByExpr` when present. The track-by expression is evaluated against a transient scope where `$index` is populated AND the item-binding (`scope[valueIdent] = item`) is set, then the result is coerced to a string for the identity key. When `trackByExpr === null`, fall back to the identity tracker from Slice 2. Update reconciliation step 6 to actually REUSE existing rows on identity match: read the row from `currentRows`, update its `$index` / `$first` / `$last` / `$middle` / `$even` / `$odd` + item-binding, MOVE its `cloneRoot` in the DOM via `parentNode.insertBefore(cloneRoot, anchor)` (anchor walks the previous row's `nextSibling`). Only items with no matching identity get a fresh `$transclude(...)`. Update TSDoc to document the reuse contract. **[Agent: typescript-framework]**
  - [ ] Extend `src/compiler/__tests__/ng-repeat.test.ts` with track-by coverage. Coverage: `track by item.id` reuses rows when ids match across collection updates; row reorder (`[A, B, C]` → `[B, A, C]`) moves DOM nodes rather than rebuilding (assert via `cloneRoot` identity equality across digests); focus preservation — focused `<input>` inside a row survives a list reorder; `track by $index` works (a list whose items legitimately repeat); `track by` accepts method calls (`track by item.identityKey()`) and property paths; `track by` evaluating to two same values throws `NgRepeatDuplicateKeyError`; an item whose tracked identity changes (e.g. `id` mutated) is treated as a tear-down + freshly-build pair on the next digest. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. **[Agent: rollup-build]**

- [ ] **Slice 5: Object iteration `(key, value) in object`**
  - [ ] Extend the reconciliation engine in `src/compiler/ng-repeat.ts` to handle object collections. Detect at reconciliation time whether the resolved collection is an array (`Array.isArray`) or a plain object (`typeof === 'object' && !Array.isArray`); both routes through the same diff machinery after normalization. Object normalization: `Object.keys(collection).sort()` (alphabetical-string sort, AngularJS-canonical), then build `[{ key, value: collection[key] }]` pairs. The per-row scope binds BOTH `parsed.keyIdent` and `parsed.valueIdent` (when keyIdent is non-null). For default identity tracking on object items, derive identity from `(key, value)` pair — since the same value can appear under multiple keys, the identity must include the key. Update TSDoc. **[Agent: typescript-framework]**
  - [ ] Extend `src/compiler/__tests__/ng-repeat.test.ts` with object-iteration coverage. Coverage: `(name, age) in people` renders one row per property with both bindings; keys visited in alphabetical-string order (`{'10': 'a', '2': 'b', '1': 'c'}` renders as `1 → c`, `10 → a`, `2 → b`); adding a property inserts a row in sorted position; removing a property removes only its row; changing a property value updates the row in place without tearing it down (identity by `(key, value)` pair → if value changes, identity changes → rebuild; explicitly accept this as the documented behavior); object-form with `track by`; mixing array-vs-object across digests (e.g. flipping `scope.collection` from `{a:1}` to `[1, 2]`) does not crash — current rows are torn down, new rows built. **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. **[Agent: rollup-build]**

- [ ] **Slice 6: `as alias` filtered-list publication on parent scope**
  - [ ] Extend the reconciliation engine in `src/compiler/ng-repeat.ts` to publish the normalized collection on the parent scope when `parsed.aliasIdent` is non-null. The write happens BEFORE row reconciliation in the same listener fire, so a sibling `<p ng-if="!visible.length">` sees the new value in the same digest. The parser already prevents the alias from colliding with the item / key / value idents in the same expression (Slice 1's `NgRepeatBadAliasError`). For array iteration the alias receives the array directly; for object iteration the alias receives the normalized `[{ key, value }]` array. Update TSDoc. **[Agent: typescript-framework]**
  - [ ] Extend `src/compiler/__tests__/ng-repeat.test.ts` with alias-publication coverage. Coverage: `<li ng-repeat="todo in todos | filter:q as visible">` followed by `<p ng-if="!visible.length">No matches.</p>` shows the empty-state message when the filter produces zero rows; `visible` updates when the filter expression changes; `visible` is published OUTSIDE the per-row scopes (sibling markup can read it); `visible.length` matches the filtered count; alias works with all combined forms (`item in list as visible`, `item in list as visible track by item.id`, `(k, v) in obj as visible`); alias name colliding with the item/key/value identifier surfaces `NgRepeatBadAliasError` at parse time (already covered in Slice 1, regression here). **[Agent: vitest-testing]**
  - [ ] Run `pnpm lint`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All prior specs pass unchanged. **[Agent: rollup-build]**

- [ ] **Slice 7: Integration parity, documentation, final regression**
  - [ ] Create `src/compiler/__tests__/spec028-parity.test.ts` — focused AngularJS-canonical regression file matching the spec 022/023/024/025/026/027 precedent. 6–8 tests: basic array iteration; `(k, v)` object iteration with alphabetical order; `track by item.id` reuse; `as visible` empty-state; combined `item in list | filter:q as visible track by item.id`; non-iterable bail; nested `ng-repeat` (`$index` shadowing); duplicate-key throws via `'$compile'`. Plus a literal `expect(EXCEPTION_HANDLER_CAUSES.length).toBe(10)` regression guard. **[Agent: vitest-testing]**
  - [ ] Create `src/compiler/__tests__/ng-repeat-integration.test.ts` — composition tests. `ng-repeat > ng-if` (row-internal toggle); `ng-repeat > ng-controller` (per-row controller); `ng-repeat > ng-include` (per-row template fetch); `ng-repeat` inside `ng-if` (mount on truthy, tear down on falsy); nested `ng-repeat` (per-item variables shadow inner-to-outer); same-element conflict (`<li ng-repeat="…" ng-if="…">` triggers `MultipleTranscludeDirectivesError` OR the known same-element terminal-cutoff gap surfaced in spec 027 — pin actually-observable behavior with an inline note for either outcome). **[Agent: vitest-testing]**
  - [ ] Update `src/compiler/README.md` — new "List iteration directive (spec 028)" section. Cover: the `transclude: 'element'` reuse from spec 027; the iterator-expression grammar (all four optional-clause combinations + the `(k, v)` LHS variant); the WeakMap-based identity tracker (and the AngularJS-`$$hashKey` divergence); the row reconciliation algorithm summary; the `as alias` parent-scope publication contract; the duplicate-detection error class; the `$animate` deferral note. **[Agent: typedoc-docs]**
  - [ ] Update `CLAUDE.md`. Amend the `./compiler` Modules row to mention the new `ngRepeat` directive registration (DI-only — matches the spec 023+ precedent), bumping the directive count from 44 to 45. Add 4–6 new "Non-obvious invariants" bullets covering: (a) the WeakMap-based identity tracker (no `$$hashKey` injection, deliberate AngularJS divergence); (b) the `as alias` write happens BEFORE row reconciliation in the same listener fire; (c) duplicate-key detection throws within the watch listener and routes via `$exceptionHandler('$compile')`, NOT `'watchListener'`; (d) `priority: 1000` makes `ng-repeat` win same-element conflicts against `ng-if` (600) and `ng-include` (400); (e) row reuse on identity match preserves DOM-node identity AND the state inside (input focus, form values, scroll position); (f) object iteration uses alphabetical-string key order. Add 3 new "Where to look when…" rows for the directive, the iterator parser, and the identity tracker. **[Agent: typedoc-docs]**
  - [ ] TSDoc audit on every new public surface — `ngRepeatDirective`'s file-level TSDoc, the four new error classes (`NgRepeatBadIteratorExpressionError`, `NgRepeatBadIdentifierError`, `NgRepeatBadAliasError`, `NgRepeatDuplicateKeyError`), the `parseIteratorExpression` helper, the `createIdentityTracker` helper. Each carries at least one runnable `@example`. **[Agent: typedoc-docs]**
  - [ ] Final regression check: `pnpm lint`, `pnpm format:check`, `pnpm typecheck`, `pnpm test`, `pnpm build`. All five gates pass. Full prior-spec test suite (002–027) passes unchanged. `EXCEPTION_HANDLER_CAUSES.length === 10` invariant held. New observable: `<li ng-repeat="…">` in all forms (basic, `(k, v)`, `track by`, `as alias`, combined) works end-to-end. **[Agent: rollup-build]**

---

## Notes for the Implementation Agent

- **No new `EXCEPTION_HANDLER_CAUSES` entry.** The four new error classes (`NgRepeatBadIteratorExpressionError`, `NgRepeatBadIdentifierError`, `NgRepeatBadAliasError`, `NgRepeatDuplicateKeyError`) all route via the existing `'$compile'` cause token. The tuple stays at 10.
- **`transclude: 'element'` is the load-bearing prerequisite — already shipped in spec 027 Slice 2.** No changes to the foundation are needed; `ng-repeat` is a straightforward consumer.
- **The reconciliation engine grows across slices 3 → 4 → 5 → 6.** Each slice adds ONE capability (default identity → track by + reuse → object iteration → alias publication). The duplicate-detection contract and the per-row scope shape are stable from Slice 3.
- **WeakMap-based identity tracker is the deliberate divergence from AngularJS.** AngularJS injects a non-enumerable `$$hashKey` onto user objects; we don't. Tests that AngularJS upstream wrote against `$$hashKey` introspection must be translated to identity-based assertions.
- **`$animate` integration is deferred to Phase 4** — synchronous DOM mutations only. Matches the spec 027 precedent for `ng-show` / `ng-hide` / `ng-class`.
- **Same-element conflict with other structural directives** (e.g. `<li ng-repeat="…" ng-if="…">`) is the documented spec 027 gap — the spec-017 same-element terminal cutoff drops the lower-priority directive instead of producing `MultipleTranscludeDirectivesError`. Slice 7's integration test pins the actually-observable behavior; resolving the gap is out of scope here.
- **Tick task checkboxes in the SAME commit as the implementation** per `CLAUDE.md` — the co-modification of `tasks.md` + `src/` in one commit is the traceability signal the audit looks for.
